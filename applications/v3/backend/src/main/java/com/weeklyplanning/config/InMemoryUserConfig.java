package com.weeklyplanning.config;

/**
 * Kept as placeholder from seed iteration. Authentication now uses JWT + database accounts.
 */
public class InMemoryUserConfig {
    private InMemoryUserConfig() {
    }
}
