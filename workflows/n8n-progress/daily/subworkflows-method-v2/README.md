This directory contains the workflows in a transition phase. Since the mistral api was having a lot of problems it was decided to run the models locally and call it in n8n. To do that there is a need to change the workflows as they are rigth now. This folder contains some modules with that transition (requirements and api). A lof of things are broken and uncoordinated now, because of this transition. This just a saving point!