{
  "name": "main-orchestrator",
  "nodes": [
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst inputBinary = { ...(item.binary ?? {}) };\n\nconst moduleOutputs = (json.moduleOutputs && typeof json.moduleOutputs === 'object' && !Array.isArray(json.moduleOutputs))\n  ? { ...json.moduleOutputs }\n  : {};\n\nconst designExcalidraw = String(json.finalDesignExcalidraw ?? json.design ?? '').trim();\nconst revisionNumber = Number(json.revisionNumber ?? 0) || 0;\nconst zipDownloadUrl = String(json.zipDownloadUrl ?? '').trim();\nconst zipDownloadToken = String(json.zipDownloadToken ?? '').trim();\nconst currentIterationNumber = Number(json.currentDesignIterationNumber ?? 0) || 0;\n\nmoduleOutputs.design = {\n  designExcalidraw,\n  revisionNumber,\n  zipDownloadUrl,\n  zipDownloadToken,\n  currentIterationNumber,\n  updatedAt: new Date().toISOString(),\n};\n\njson.moduleOutputs = moduleOutputs;\njson.finalDesign = designExcalidraw;\njson.finalDesignExcalidraw = designExcalidraw;\njson.finalDesignRevisionNumber = revisionNumber;\njson.finalDesignZipDownloadUrl = zipDownloadUrl;\n\ndelete json.finalDesignDsl;\ndelete json.designDsl;\ndelete json.firstIterationDesignDsl;\n\nconst requirementsBinary = inputBinary.requirements_final ?? inputBinary.requirements;\nconst designBinary = inputBinary.design_final ?? inputBinary.design;\nconst outputBinary = {};\nif (requirementsBinary) {\n  outputBinary.requirements = requirementsBinary;\n}\nif (designBinary) {\n  outputBinary.design = designBinary;\n}\n\nreturn [{\n  json,\n  ...(Object.keys(outputBinary).length ? { binary: outputBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        54576,
        11840
      ],
      "id": "409f666a-53d5-4856-a887-9e1ab0bc68c6",
      "name": "DESIGN - End"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "8b489161-f88c-4771-8798-3189a6307c71",
      "name": "DESIGN - History Merge",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        53312,
        11824
      ]
    },
    {
      "parameters": {
        "defineForm": "json",
        "jsonOutput": "={{ $json.designReviewFormJson }}",
        "options": {
          "formTitle": "Human Review: Design Draft",
          "formDescription": "={{ $json.designReviewDescriptionHtml }}",
          "buttonLabel": "Submit"
        }
      },
      "id": "90309383-fc03-49ae-9992-b6881f657cb0",
      "name": "DESIGN - Review",
      "type": "n8n-nodes-base.form",
      "typeVersion": 2.5,
      "position": [
        53744,
        11888
      ],
      "webhookId": "9d6db7aa-72a2-4a75-9df1-fcd690176c4c"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "75b58250-264b-4f20-9e5c-120a79b271c4",
      "name": "DESIGN - Review Context",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        53952,
        11840
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "restore-older-design",
              "leftValue": "={{ $json[\"Restore older iteration\"] }}",
              "rightValue": "Yes",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "6ba080a7-8e60-4dc2-81c3-450145298824",
      "name": "DESIGN - Restore Older Iteration?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        54160,
        11840
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "approved-design",
              "leftValue": "={{ $json.Approved }}",
              "rightValue": "approved",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "42af5d97-7feb-48e1-a7b6-02543b5d49d6",
      "name": "DESIGN - Approved?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        54368,
        11856
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = {};\nif (Array.isArray(item.json?.designHistory)) {\n  json.designHistory = item.json.designHistory;\n}\n\nconst binary = {};\nfor (const [name, value] of Object.entries(item.binary ?? {})) {\n  if (name === 'requirements' || /^design_iteration_\\d+$/.test(name)) {\n    binary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(binary).length ? { binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        53056,
        11920
      ],
      "id": "15293928-2f42-4831-8870-14c55b3edc70",
      "name": "DESIGN - History Context"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildDesignReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Design ZIP (PNGs)</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the visual design.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Design ZIP (PNG screens/modals/dialogs), then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst existingHistory = Array.isArray(json.designHistory)\n  ? json.designHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst normalizedHistory = existingHistory.map((entry, index) => ({\n  iterationNumber: Number(entry.iterationNumber ?? index + 1) || (index + 1),\n  revisionNumber: Number(entry.revisionNumber ?? 0) || 0,\n  designDsl: String(entry.designDsl ?? '').trim(),\n  zipDownloadUrl: String(entry.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(entry.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(entry.downloadLinkHtml ?? '').trim(),\n  binaryPropertyName: String(entry.binaryPropertyName ?? '').trim(),\n}));\n\nconst currentEntry = {\n  revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n  designDsl: String(json.designDsl ?? '').trim(),\n  zipDownloadUrl: String(json.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(json.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(json.downloadLinkHtml ?? '').trim(),\n};\n\nconst isSameEntry = (entry) =>\n  entry.revisionNumber === currentEntry.revisionNumber &&\n  entry.zipDownloadToken === currentEntry.zipDownloadToken &&\n  entry.zipDownloadUrl === currentEntry.zipDownloadUrl &&\n  entry.designDsl === currentEntry.designDsl;\n\nlet storedEntry = normalizedHistory.find(isSameEntry);\n\nconst usedBinaryPropertyNames = new Set(\n  normalizedHistory\n    .map((entry) => String(entry.binaryPropertyName ?? '').trim())\n    .filter(Boolean),\n);\n\nif (!storedEntry) {\n  const nextIterationNumber = normalizedHistory.reduce(\n    (maxValue, entry) => Math.max(maxValue, Number(entry.iterationNumber ?? 0) || 0),\n    0,\n  ) + 1;\n\n  let binaryPropertyName = '';\n  if (binary.design) {\n    let suffix = nextIterationNumber;\n    do {\n      binaryPropertyName = `design_iteration_${suffix}`;\n      suffix += 1;\n    } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n    binary[binaryPropertyName] = binary.design;\n  }\n\n  storedEntry = {\n    iterationNumber: nextIterationNumber,\n    ...currentEntry,\n    binaryPropertyName,\n  };\n  normalizedHistory.push(storedEntry);\n} else if (!storedEntry.binaryPropertyName && binary.design) {\n  let suffix = Number(storedEntry.iterationNumber ?? normalizedHistory.length + 1) || (normalizedHistory.length + 1);\n  let binaryPropertyName = '';\n  do {\n    binaryPropertyName = `design_iteration_${suffix}`;\n    suffix += 1;\n  } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n  binary[binaryPropertyName] = binary.design;\n  storedEntry.binaryPropertyName = binaryPropertyName;\n}\n\njson.designHistory = normalizedHistory;\njson.currentDesignIterationNumber = Number(storedEntry.iterationNumber ?? 0) || 0;\n\nconst reviewAssets = buildDesignReviewAssets({\n  history: normalizedHistory,\n  currentIterationNumber: json.currentDesignIterationNumber,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: '',\n});\njson.designReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.designReviewFormJson = reviewAssets.formJson;\n\ndelete json.designHistoryHtml;\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'design' || /^design_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        53520,
        11824
      ],
      "id": "09ed9b54-2f16-4ea8-8566-e4fec637f03b",
      "name": "DESIGN - Store Iterations"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\nconst history = Array.isArray(json.designHistory)\n  ? json.designHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildDesignReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Design ZIP (PNGs)</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the visual design.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Design ZIP (PNG screens/modals/dialogs), then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst selectionLabel = String(json['Iteration to restore'] ?? '').trim();\nconst selectionMatch = selectionLabel.match(/Iteration\\s+(\\d+)/i);\nconst selectedIterationNumber = selectionMatch ? Number.parseInt(selectionMatch[1], 10) : Number.NaN;\nconst currentIterationNumber = Number(json.currentDesignIterationNumber ?? 0) || 0;\nconst selectedEntry = history.find((entry) => Number(entry.iterationNumber ?? 0) === selectedIterationNumber);\n\nif (selectedEntry && Number(selectedEntry.iterationNumber ?? 0) !== currentIterationNumber) {\n  json.designDsl = String(selectedEntry.designDsl ?? json.designDsl ?? '').trim();\n  json.revisionNumber = Number(selectedEntry.revisionNumber ?? json.revisionNumber ?? 0) || 0;\n  json.zipDownloadUrl = String(selectedEntry.zipDownloadUrl ?? json.zipDownloadUrl ?? '').trim();\n  json.zipDownloadToken = String(selectedEntry.zipDownloadToken ?? json.zipDownloadToken ?? '').trim();\n  json.downloadLinkHtml = String(selectedEntry.downloadLinkHtml ?? json.downloadLinkHtml ?? '').trim();\n  json.currentDesignIterationNumber = Number(selectedEntry.iterationNumber ?? json.currentDesignIterationNumber ?? 0) || 0;\n\n  const binaryPropertyName = String(selectedEntry.binaryPropertyName ?? '').trim();\n  if (binaryPropertyName && binary[binaryPropertyName]) {\n    binary.design = binary[binaryPropertyName];\n  }\n\n  delete json.restoreSelectionError;\n} else {\n  json.restoreSelectionError = 'Select one of the previously generated iterations to restore it.';\n}\n\nconst reviewAssets = buildDesignReviewAssets({\n  history,\n  currentIterationNumber: Number(json.currentDesignIterationNumber ?? 0) || 0,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: String(json.restoreSelectionError ?? '').trim(),\n});\njson.designReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.designReviewFormJson = reviewAssets.formJson;\n\ndelete json.designHistoryHtml;\ndelete json.reviewMode;\ndelete json.feedback;\ndelete json.Feedback;\ndelete json.Approved;\ndelete json.Attachments;\ndelete json['Restore older iteration'];\ndelete json['Iteration to restore'];\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'design' || /^design_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        54144,
        12080
      ],
      "id": "82a06ae9-eebc-433a-989b-e0f4b7fd0733",
      "name": "DESIGN - Restore Selected Iteration"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nreturn [{\n  json: {\n    ...(item.json ?? {}),\n    reviewMode: 'refine',\n  },\n  ...(item.binary && Object.keys(item.binary).length ? { binary: item.binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        53328,
        12096
      ],
      "id": "ff0687c3-1c20-4c6e-888b-ee750a8a5b20",
      "name": "DESIGN - Prepare Refine Input"
    },
    {
      "parameters": {
        "content": "## Requirements Generation Module\n\nWith the **user instructions** creates a **structured requirements document**.",
        "height": 720,
        "width": 1744,
        "color": "#DBEAFE"
      },
      "type": "n8n-nodes-base.stickyNote",
      "position": [
        50960,
        10752
      ],
      "typeVersion": 1,
      "id": "89e38970-a462-4c18-b543-f42b75f85a28",
      "name": "Sticky Note1"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        52848,
        11824
      ],
      "id": "3194a0f9-88ba-4f38-acad-8fd3880605e2",
      "name": "DESIGN - Start"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "approved-requirements",
              "leftValue": "={{ $json.Approved }}",
              "rightValue": "approved",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "884596b1-e69c-45f3-93a0-af1667d43808",
      "name": "REQ - Approved?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        52464,
        10976
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "restore-older-requirements",
              "leftValue": "={{ $json[\"Restore older iteration\"] }}",
              "rightValue": "Yes",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "f34c261c-ecde-4040-b5da-30ee4385ae65",
      "name": "REQ - Restore Older Iteration?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        52256,
        10960
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\nconst history = Array.isArray(json.requirementsHistory)\n  ? json.requirementsHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildRequirementsReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Requirements ZIP</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change. Reference line numbers from the draft above when possible.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Requirements ZIP (markdown + PDF), then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst selectionLabel = String(json['Iteration to restore'] ?? '').trim();\nconst selectionMatch = selectionLabel.match(/Iteration\\s+(\\d+)/i);\nconst selectedIterationNumber = selectionMatch ? Number.parseInt(selectionMatch[1], 10) : Number.NaN;\nconst currentIterationNumber = Number(json.currentRequirementsIterationNumber ?? 0) || 0;\nconst selectedEntry = history.find((entry) => Number(entry.iterationNumber ?? 0) === selectedIterationNumber);\n\nif (selectedEntry && Number(selectedEntry.iterationNumber ?? 0) !== currentIterationNumber) {\n  json.requirements = String(selectedEntry.requirements ?? json.requirements ?? '').trim();\n  json.revisionNumber = Number(selectedEntry.revisionNumber ?? json.revisionNumber ?? 0) || 0;\n  json.zipDownloadUrl = String(selectedEntry.zipDownloadUrl ?? json.zipDownloadUrl ?? '').trim();\n  json.zipDownloadToken = String(selectedEntry.zipDownloadToken ?? json.zipDownloadToken ?? '').trim();\n  json.downloadLinkHtml = String(selectedEntry.downloadLinkHtml ?? json.downloadLinkHtml ?? '').trim();\n  json.currentRequirementsIterationNumber = Number(selectedEntry.iterationNumber ?? json.currentRequirementsIterationNumber ?? 0) || 0;\n\n  const binaryPropertyName = String(selectedEntry.binaryPropertyName ?? '').trim();\n  if (binaryPropertyName && binary[binaryPropertyName]) {\n    binary.requirements = binary[binaryPropertyName];\n  }\n\n  delete json.restoreSelectionError;\n} else {\n  json.restoreSelectionError = 'Select one of the previously generated iterations to restore it.';\n}\n\nconst reviewAssets = buildRequirementsReviewAssets({\n  history,\n  currentIterationNumber: Number(json.currentRequirementsIterationNumber ?? 0) || 0,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: String(json.restoreSelectionError ?? '').trim(),\n});\njson.requirementsReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.requirementsReviewFormJson = reviewAssets.formJson;\n\ndelete json.requirementsHistoryHtml;\ndelete json.reviewMode;\ndelete json.feedback;\ndelete json.Feedback;\ndelete json.Approved;\ndelete json.Attachments;\ndelete json['Restore older iteration'];\ndelete json['Iteration to restore'];\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || /^requirements_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        52256,
        11184
      ],
      "id": "86bb2ddf-2c24-4be1-b0de-ef8717537db6",
      "name": "REQ - Restore Selected Iteration"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "262fdf4a-d161-4986-9a45-8ac4046bcb67",
      "name": "REQ - Review Context",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        52048,
        10960
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildRequirementsReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Requirements ZIP</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change. Reference line numbers from the draft above when possible.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Requirements ZIP (markdown + PDF), then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst existingHistory = Array.isArray(json.requirementsHistory)\n  ? json.requirementsHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst normalizedHistory = existingHistory.map((entry, index) => ({\n  iterationNumber: Number(entry.iterationNumber ?? index + 1) || (index + 1),\n  revisionNumber: Number(entry.revisionNumber ?? 0) || 0,\n  requirements: String(entry.requirements ?? '').trim(),\n  zipDownloadUrl: String(entry.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(entry.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(entry.downloadLinkHtml ?? '').trim(),\n  binaryPropertyName: String(entry.binaryPropertyName ?? '').trim(),\n}));\n\nconst currentEntry = {\n  revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n  requirements: String(json.requirements ?? '').trim(),\n  zipDownloadUrl: String(json.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(json.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(json.downloadLinkHtml ?? '').trim(),\n};\n\nconst isSameEntry = (entry) =>\n  entry.revisionNumber === currentEntry.revisionNumber &&\n  entry.zipDownloadToken === currentEntry.zipDownloadToken &&\n  entry.zipDownloadUrl === currentEntry.zipDownloadUrl &&\n  entry.requirements === currentEntry.requirements;\n\nlet storedEntry = normalizedHistory.find(isSameEntry);\n\nconst usedBinaryPropertyNames = new Set(\n  normalizedHistory\n    .map((entry) => String(entry.binaryPropertyName ?? '').trim())\n    .filter(Boolean),\n);\n\nif (!storedEntry) {\n  const nextIterationNumber = normalizedHistory.reduce(\n    (maxValue, entry) => Math.max(maxValue, Number(entry.iterationNumber ?? 0) || 0),\n    0,\n  ) + 1;\n\n  let binaryPropertyName = '';\n  if (binary.requirements) {\n    let suffix = nextIterationNumber;\n    do {\n      binaryPropertyName = `requirements_iteration_${suffix}`;\n      suffix += 1;\n    } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n    binary[binaryPropertyName] = binary.requirements;\n  }\n\n  storedEntry = {\n    iterationNumber: nextIterationNumber,\n    ...currentEntry,\n    binaryPropertyName,\n  };\n  normalizedHistory.push(storedEntry);\n} else if (!storedEntry.binaryPropertyName && binary.requirements) {\n  let suffix = Number(storedEntry.iterationNumber ?? normalizedHistory.length + 1) || (normalizedHistory.length + 1);\n  let binaryPropertyName = '';\n  do {\n    binaryPropertyName = `requirements_iteration_${suffix}`;\n    suffix += 1;\n  } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n  binary[binaryPropertyName] = binary.requirements;\n  storedEntry.binaryPropertyName = binaryPropertyName;\n}\n\njson.requirementsHistory = normalizedHistory;\njson.currentRequirementsIterationNumber = Number(storedEntry.iterationNumber ?? 0) || 0;\n\nconst reviewAssets = buildRequirementsReviewAssets({\n  history: normalizedHistory,\n  currentIterationNumber: json.currentRequirementsIterationNumber,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: '',\n});\njson.requirementsReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.requirementsReviewFormJson = reviewAssets.formJson;\n\ndelete json.requirementsHistoryHtml;\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || /^requirements_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        51584,
        10944
      ],
      "id": "1b614738-6cb9-4112-8ec5-dd5fc0f374c6",
      "name": "REQ - Store Iterations"
    },
    {
      "parameters": {
        "defineForm": "json",
        "jsonOutput": "={{ $json.requirementsReviewFormJson }}",
        "options": {
          "formTitle": "Human Review: Requirements Draft",
          "formDescription": "={{ $json.requirementsReviewDescriptionHtml }}",
          "buttonLabel": "Submit"
        }
      },
      "id": "03278594-5f37-4a88-9efa-85d36537c01d",
      "name": "REQ - Review",
      "type": "n8n-nodes-base.form",
      "typeVersion": 2.5,
      "position": [
        51824,
        11008
      ],
      "webhookId": "dc399c82-be8d-482a-ab57-eec65d97acde"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "cc9b4968-2c5d-4bf6-bb48-22a12f057809",
      "name": "REQ - History Merge",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        51392,
        10944
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nreturn [{\n  json: {\n    ...(item.json ?? {}),\n    reviewMode: 'refine',\n  },\n  ...(item.binary && Object.keys(item.binary).length ? { binary: item.binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        51344,
        11200
      ],
      "id": "7838406e-53c5-409e-bde7-d6355e11bcb5",
      "name": "REQ - Prepare Refine Input"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = {};\nif (Array.isArray(item.json?.requirementsHistory)) {\n  json.requirementsHistory = item.json.requirementsHistory;\n}\n\nconst binary = {};\nfor (const [name, value] of Object.entries(item.binary ?? {})) {\n  if (/^requirements_iteration_\\d+$/.test(name)) {\n    binary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(binary).length ? { binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        51120,
        11040
      ],
      "id": "d2c23d10-64b9-499e-8dc5-8f606ebf84f1",
      "name": "REQ - History Context"
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "XAdWquYoHZ6lvPvT",
          "mode": "list",
          "cachedResultUrl": "/workflow/XAdWquYoHZ6lvPvT",
          "cachedResultName": "requirements-workflow"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {},
          "matchingColumns": [],
          "schema": [],
          "attemptToConvertTypes": false,
          "convertFieldsToString": true
        },
        "options": {}
      },
      "id": "aeaa64c6-d0dd-4ac7-a69e-364e2b2a83b3",
      "name": "REQ - Create Requirements",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        51120,
        10848
      ]
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        50912,
        10944
      ],
      "id": "6f2a1fb7-73ea-43c4-9b60-3df87ac03984",
      "name": "REQ - Start"
    },
    {
      "parameters": {
        "formTitle": "AI Application Generation Orchestrator",
        "formDescription": "\nThis workflow uses artificial intelligence to transform your description into structured software artefacts.\nThe process follows a modular pipeline where each stage builds a new artefact with human validation steps ensuring quality and correctness.\nThe clarity of your description directly impacts the quality of the generated system.",
        "formFields": {
          "values": [
            {
              "fieldType": "html",
              "elementName": "introPanel",
              "html": "<div style=\"margin:0 0 12px 0;padding:0;\">\n  <div style=\"padding:22px 24px;border:1px solid #d7e2ea;border-radius:18px;background:linear-gradient(135deg,#f8fbfd 0%,#eef5fa 100%);box-shadow:0 10px 24px rgba(27,63,84,0.06);\">\n    <div style=\"font-size:14px;line-height:1.7;color:#395463;margin-bottom:16px;\">\n      This form starts the workflow by capturing the essential context needed to produce better artefacts. Clear, concrete and precise information will lead to better output.\n      <p></p>\n    </div>\n    <div style=\"display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;\">\n      <div style=\"padding:12px 14px;border-radius:12px;background:#ffffff;border:1px solid #dbe7ee;color:#36505d;\">\n        Please insert the zip that explains the idea you have for this application.\n        <p></p>\n      </div>\n    </div>\n  </div>\n</div>"
            },
            {
              "fieldLabel": "Requirements ZIP",
              "fieldType": "file",
              "fieldName": "requirementsZip",
              "multipleFiles": false,
              "acceptFileTypes": ".zip"
            },
            {
              "fieldType": "html",
              "elementName": "finalAdvice",
              "html": "<div style=\"margin-top:6px;padding:16px 18px;border:1px solid #d7e2ea;border-radius:14px;background:#f9fbfc;color:#405661;line-height:1.65;\"><strong style=\"color:#1f5872;\">Before you submit</strong><br/>Review what you wrote so you are sure there are no inconsistencies and no missing information.</div>"
            }
          ]
        },
        "options": {
          "buttonLabel": "Submit Form",
          "path": "4c49925b-e0e8-4a93-bc52-61b9f0d01453",
          "useWorkflowTimezone": true
        }
      },
      "type": "n8n-nodes-base.formTrigger",
      "typeVersion": 2.5,
      "position": [
        50672,
        10944
      ],
      "id": "a2c479f3-d398-411f-a081-4133f060269b",
      "name": "Start",
      "webhookId": "4c49925b-e0e8-4a93-bc52-61b9f0d01453",
      "notesInFlow": false
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "jC1AUZvgAX3hPGaX",
          "mode": "list",
          "cachedResultUrl": "/workflow/jC1AUZvgAX3hPGaX",
          "cachedResultName": "design-workflow"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {},
          "matchingColumns": [],
          "schema": [],
          "attemptToConvertTypes": false,
          "convertFieldsToString": true
        },
        "options": {}
      },
      "id": "9c800763-2690-4b24-85a0-501fbc1e6aa0",
      "name": "DESIGN - Create Design",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        53056,
        11728
      ]
    },
    {
      "parameters": {
        "content": "## Design Generation Module\n\nWith the **structured requirements document** creates a **design json file**.",
        "height": 720,
        "width": 1744,
        "color": "#EDE9FE"
      },
      "type": "n8n-nodes-base.stickyNote",
      "position": [
        52896,
        11632
      ],
      "typeVersion": 1,
      "id": "ac99c7d8-2695-438f-a171-149e5a4dccc3",
      "name": "Sticky Note"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        52880,
        10960
      ],
      "id": "09a1e222-a661-4068-ba4b-4ef1aa8d9dc1",
      "name": "Flow Node1"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        54784,
        12528
      ],
      "id": "2c09797b-8ec9-4df5-90e1-25ef7f1a5ef0",
      "name": "API - Start"
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "PLCei3E4WVIlx4sf",
          "mode": "list",
          "cachedResultUrl": "/workflow/PLCei3E4WVIlx4sf",
          "cachedResultName": "api-workflow"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {},
          "matchingColumns": [],
          "schema": [],
          "attemptToConvertTypes": false,
          "convertFieldsToString": true
        },
        "options": {}
      },
      "id": "347e801b-4951-443e-adfb-19fc8ce796e8",
      "name": "API - Create API",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        55008,
        12432
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = {};\nif (Array.isArray(item.json?.apiHistory)) {\n  json.apiHistory = item.json.apiHistory;\n}\n\nconst binary = {};\nfor (const [name, value] of Object.entries(item.binary ?? {})) {\n  if (name === 'requirements' || name === 'design' || /^api_iteration_\\d+$/.test(name)) {\n    binary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(binary).length ? { binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        55008,
        12624
      ],
      "id": "f536ef0f-6d69-4eaa-8d4d-8a5bad748803",
      "name": "API - History Context"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "c78f136c-bfae-4579-81b1-d2e35acaf6ea",
      "name": "API - History Merge",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        55264,
        12528
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildApiReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current API ZIP (YAML + PDF)</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the API specification.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current API ZIP (YAML + PDF), then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst existingHistory = Array.isArray(json.apiHistory)\n  ? json.apiHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst normalizedHistory = existingHistory.map((entry, index) => ({\n  iterationNumber: Number(entry.iterationNumber ?? index + 1) || (index + 1),\n  revisionNumber: Number(entry.revisionNumber ?? 0) || 0,\n  openapiYaml: String(entry.openapiYaml ?? entry.response ?? '').trim(),\n  zipDownloadUrl: String(entry.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(entry.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(entry.downloadLinkHtml ?? '').trim(),\n  binaryPropertyName: String(entry.binaryPropertyName ?? '').trim(),\n}));\n\nconst currentEntry = {\n  revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n  openapiYaml: String(json.openapiYaml ?? json.response ?? '').trim(),\n  zipDownloadUrl: String(json.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(json.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(json.downloadLinkHtml ?? '').trim(),\n};\n\nconst isSameEntry = (entry) =>\n  entry.revisionNumber === currentEntry.revisionNumber &&\n  entry.zipDownloadToken === currentEntry.zipDownloadToken &&\n  entry.zipDownloadUrl === currentEntry.zipDownloadUrl &&\n  entry.openapiYaml === currentEntry.openapiYaml;\n\nlet storedEntry = normalizedHistory.find(isSameEntry);\n\nconst usedBinaryPropertyNames = new Set(\n  normalizedHistory\n    .map((entry) => String(entry.binaryPropertyName ?? '').trim())\n    .filter(Boolean),\n);\n\nif (!storedEntry) {\n  const nextIterationNumber = normalizedHistory.reduce(\n    (maxValue, entry) => Math.max(maxValue, Number(entry.iterationNumber ?? 0) || 0),\n    0,\n  ) + 1;\n\n  let binaryPropertyName = '';\n  if (binary.openapiSpec) {\n    let suffix = nextIterationNumber;\n    do {\n      binaryPropertyName = `api_iteration_${suffix}`;\n      suffix += 1;\n    } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n    binary[binaryPropertyName] = binary.openapiSpec;\n  }\n\n  storedEntry = {\n    iterationNumber: nextIterationNumber,\n    ...currentEntry,\n    binaryPropertyName,\n  };\n  normalizedHistory.push(storedEntry);\n} else if (!storedEntry.binaryPropertyName && binary.openapiSpec) {\n  let suffix = Number(storedEntry.iterationNumber ?? normalizedHistory.length + 1) || (normalizedHistory.length + 1);\n  let binaryPropertyName = '';\n  do {\n    binaryPropertyName = `api_iteration_${suffix}`;\n    suffix += 1;\n  } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n  binary[binaryPropertyName] = binary.openapiSpec;\n  storedEntry.binaryPropertyName = binaryPropertyName;\n}\n\njson.apiHistory = normalizedHistory;\njson.currentApiIterationNumber = Number(storedEntry.iterationNumber ?? 0) || 0;\n\nconst reviewAssets = buildApiReviewAssets({\n  history: normalizedHistory,\n  currentIterationNumber: json.currentApiIterationNumber,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: '',\n});\njson.apiReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.apiReviewFormJson = reviewAssets.formJson;\n\ndelete json.apiHistoryHtml;\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'design' || name === 'openapiSpec' || /^api_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        55472,
        12528
      ],
      "id": "d7b4d0d5-18ac-4ad2-a730-ccc2d9ef8d5f",
      "name": "API - Store Iterations"
    },
    {
      "parameters": {
        "defineForm": "json",
        "jsonOutput": "={{ $json.apiReviewFormJson }}",
        "options": {
          "formTitle": "Human Review: API Draft",
          "formDescription": "={{ $json.apiReviewDescriptionHtml }}",
          "buttonLabel": "Submit"
        }
      },
      "id": "0fd6f266-3e6d-4a29-aa8b-025472fe7e00",
      "name": "API - Review",
      "type": "n8n-nodes-base.form",
      "typeVersion": 2.5,
      "position": [
        55696,
        12592
      ],
      "webhookId": "9d6db7aa-72a2-4a75-9df1-fcd690176c4c"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "08d9febd-271b-4576-b6e5-1cdd4e9eca5d",
      "name": "API - Review Context",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        55904,
        12544
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "restore-older-api",
              "leftValue": "={{ $json[\"Restore older iteration\"] }}",
              "rightValue": "Yes",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "191e54c3-06c3-4742-b0fd-bc88c2cc2ab3",
      "name": "API - Restore Older Iteration?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        56112,
        12544
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\nconst history = Array.isArray(json.apiHistory)\n  ? json.apiHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildApiReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current API ZIP (YAML + PDF)</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the API specification.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current API ZIP (YAML + PDF), then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst selectionLabel = String(json['Iteration to restore'] ?? '').trim();\nconst selectionMatch = selectionLabel.match(/Iteration\\s+(\\d+)/i);\nconst selectedIterationNumber = selectionMatch ? Number.parseInt(selectionMatch[1], 10) : Number.NaN;\nconst currentIterationNumber = Number(json.currentApiIterationNumber ?? 0) || 0;\nconst selectedEntry = history.find((entry) => Number(entry.iterationNumber ?? 0) === selectedIterationNumber);\n\nif (selectedEntry && Number(selectedEntry.iterationNumber ?? 0) !== currentIterationNumber) {\n  json.openapiYaml = String(selectedEntry.openapiYaml ?? selectedEntry.response ?? json.openapiYaml ?? json.response ?? '').trim();\n  json.response = json.openapiYaml;\n  json.revisionNumber = Number(selectedEntry.revisionNumber ?? json.revisionNumber ?? 0) || 0;\n  json.zipDownloadUrl = String(selectedEntry.zipDownloadUrl ?? json.zipDownloadUrl ?? '').trim();\n  json.zipDownloadToken = String(selectedEntry.zipDownloadToken ?? json.zipDownloadToken ?? '').trim();\n  json.downloadLinkHtml = String(selectedEntry.downloadLinkHtml ?? json.downloadLinkHtml ?? '').trim();\n  json.currentApiIterationNumber = Number(selectedEntry.iterationNumber ?? json.currentApiIterationNumber ?? 0) || 0;\n\n  const binaryPropertyName = String(selectedEntry.binaryPropertyName ?? '').trim();\n  if (binaryPropertyName && binary[binaryPropertyName]) {\n    binary.openapiSpec = binary[binaryPropertyName];\n  }\n\n  delete json.restoreSelectionError;\n} else {\n  json.restoreSelectionError = 'Select one of the previously generated iterations to restore it.';\n}\n\nconst reviewAssets = buildApiReviewAssets({\n  history,\n  currentIterationNumber: Number(json.currentApiIterationNumber ?? 0) || 0,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: String(json.restoreSelectionError ?? '').trim(),\n});\njson.apiReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.apiReviewFormJson = reviewAssets.formJson;\n\ndelete json.apiHistoryHtml;\ndelete json.reviewMode;\ndelete json.feedback;\ndelete json.Feedback;\ndelete json.Approved;\ndelete json.Attachments;\ndelete json['Restore older iteration'];\ndelete json['Iteration to restore'];\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'design' || name === 'openapiSpec' || /^api_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        56096,
        12784
      ],
      "id": "5dbbcd74-7920-45c6-ad8f-486d7bbf3da0",
      "name": "API - Restore Selected Iteration"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "approved-api",
              "leftValue": "={{ $json.Approved }}",
              "rightValue": "approved",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "f171db65-f90f-4197-a2eb-f631ed211a1b",
      "name": "API - Approved?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        56320,
        12560
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nreturn [{\n  json: {\n    ...(item.json ?? {}),\n    reviewMode: 'refine',\n  },\n  ...(item.binary && Object.keys(item.binary).length ? { binary: item.binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        55280,
        12800
      ],
      "id": "c8b5354e-f301-4ba8-af34-3cf69e524acd",
      "name": "API - Prepare Refine Input"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst inputBinary = { ...(item.binary ?? {}) };\n\nconst moduleOutputs = (json.moduleOutputs && typeof json.moduleOutputs === 'object' && !Array.isArray(json.moduleOutputs))\n  ? { ...json.moduleOutputs }\n  : {};\n\nconst openapiYaml = String(json.openapiYaml ?? json.response ?? '').trim();\nconst revisionNumber = Number(json.revisionNumber ?? 0) || 0;\nconst zipDownloadUrl = String(json.zipDownloadUrl ?? '').trim();\nconst zipDownloadToken = String(json.zipDownloadToken ?? '').trim();\nconst currentIterationNumber = Number(json.currentApiIterationNumber ?? 0) || 0;\n\nmoduleOutputs.api = {\n  openapiYaml,\n  revisionNumber,\n  zipDownloadUrl,\n  zipDownloadToken,\n  currentIterationNumber,\n  updatedAt: new Date().toISOString(),\n};\n\njson.moduleOutputs = moduleOutputs;\njson.finalOpenapiYaml = openapiYaml;\njson.finalApiRevisionNumber = revisionNumber;\njson.finalApiZipDownloadUrl = zipDownloadUrl;\n\nconst requirementsBinary = inputBinary.requirements_final ?? inputBinary.requirements;\nconst designBinary = inputBinary.design_final ?? inputBinary.design;\nlet openapiBinary = inputBinary.openapiSpec_final ?? inputBinary.openapiSpec;\nif (!openapiBinary && openapiYaml) {\n  openapiBinary = {\n    data: Buffer.from(openapiYaml, 'utf8').toString('base64'),\n    fileName: 'openapi.yaml',\n    mimeType: 'application/yaml',\n  };\n}\n\nconst outputBinary = {};\nif (requirementsBinary) {\n  outputBinary.requirements = requirementsBinary;\n}\nif (designBinary) {\n  outputBinary.design = designBinary;\n}\nif (openapiBinary) {\n  outputBinary.api = openapiBinary;\n  outputBinary.openapiSpec = openapiBinary;\n}\n\nreturn [{\n  json,\n  ...(Object.keys(outputBinary).length ? { binary: outputBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        56528,
        12544
      ],
      "id": "afebcee1-be1c-41de-a651-4ae621bd0f2b",
      "name": "API - End"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        54848,
        11840
      ],
      "id": "0d44a44a-05ee-4ecb-b766-73d5e2cf742e",
      "name": "Flow Node2"
    },
    {
      "parameters": {
        "content": "## API Generation Module\n\nWith the **structured requirements** and **UI design artifacts** creates an **OpenAPI specification**.",
        "height": 720,
        "width": 1744,
        "color": "#DCFCE7"
      },
      "type": "n8n-nodes-base.stickyNote",
      "position": [
        54832,
        12336
      ],
      "typeVersion": 1,
      "id": "239faab3-9eed-4546-a47a-f67b65d5cf31",
      "name": "Sticky Note2"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst inputBinary = { ...(item.binary ?? {}) };\n\nconst moduleOutputs = (json.moduleOutputs && typeof json.moduleOutputs === 'object' && !Array.isArray(json.moduleOutputs))\n  ? { ...json.moduleOutputs }\n  : {};\n\nconst requirementsText = String(json.requirements ?? '').trim();\nconst revisionNumber = Number(json.revisionNumber ?? 0) || 0;\nconst zipDownloadUrl = String(json.zipDownloadUrl ?? '').trim();\nconst zipDownloadToken = String(json.zipDownloadToken ?? '').trim();\nconst currentIterationNumber = Number(json.currentRequirementsIterationNumber ?? 0) || 0;\n\nmoduleOutputs.requirements = {\n  text: requirementsText,\n  revisionNumber,\n  zipDownloadUrl,\n  zipDownloadToken,\n  currentIterationNumber,\n  updatedAt: new Date().toISOString(),\n};\n\njson.moduleOutputs = moduleOutputs;\njson.finalRequirements = requirementsText;\njson.finalRequirementsRevisionNumber = revisionNumber;\njson.finalRequirementsZipDownloadUrl = zipDownloadUrl;\n\nconst requirementsBinary = inputBinary.requirements_final ?? inputBinary.requirements;\nconst outputBinary = {};\nif (requirementsBinary) {\n  outputBinary.requirements = requirementsBinary;\n}\n\nreturn [{\n  json,\n  ...(Object.keys(outputBinary).length ? { binary: outputBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        52656,
        10960
      ],
      "id": "0cde9241-f760-46b4-b9d6-4bc12d9112b8",
      "name": "REQ - End"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        56976,
        12016
      ],
      "id": "29bba121-0e3c-4f2d-98e3-aaeadd1fdd2a",
      "name": "BE - Start"
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "kHkMVIEmwo2bg3Qw",
          "mode": "list",
          "cachedResultUrl": "/workflow/kHkMVIEmwo2bg3Qw",
          "cachedResultName": "backend-workflow"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {},
          "matchingColumns": [],
          "schema": [],
          "attemptToConvertTypes": false,
          "convertFieldsToString": true
        },
        "options": {}
      },
      "id": "e709b6da-c21e-47e9-a004-495caae9a1c4",
      "name": "BE - Create Backend",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        57376,
        11920
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = {};\nif (Array.isArray(item.json?.backendHistory)) {\n  json.backendHistory = item.json.backendHistory;\n}\n\nconst binary = {};\nfor (const [name, value] of Object.entries(item.binary ?? {})) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'backendZip' || /^backend_iteration_\\d+$/.test(name)) {\n    binary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(binary).length ? { binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        57376,
        12112
      ],
      "id": "4ac77fb8-48be-4923-980c-6cd43b83b9c3",
      "name": "BE - History Context"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "b0403828-a51c-42b5-8dc5-7283ae82d061",
      "name": "BE - History Merge",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        57632,
        12016
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildBackendReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Backend ZIP</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the backend package.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Backend ZIP, then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst existingHistory = Array.isArray(json.backendHistory)\n  ? json.backendHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst normalizedHistory = existingHistory.map((entry, index) => ({\n  iterationNumber: Number(entry.iterationNumber ?? index + 1) || (index + 1),\n  revisionNumber: Number(entry.revisionNumber ?? 0) || 0,\n  backendStructure: String(entry.backendStructure ?? entry.response ?? '').trim(),\n  zipDownloadUrl: String(entry.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(entry.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(entry.downloadLinkHtml ?? '').trim(),\n  binaryPropertyName: String(entry.binaryPropertyName ?? '').trim(),\n  fileCount: Number(entry.fileCount ?? 0) || 0,\n  files: Array.isArray(entry.files) ? entry.files : [],\n}));\n\nconst currentEntry = {\n  revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n  backendStructure: String(json.backendStructure ?? json.response ?? '').trim(),\n  zipDownloadUrl: String(json.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(json.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(json.downloadLinkHtml ?? '').trim(),\n  fileCount: Number(json.fileCount ?? 0) || 0,\n  files: Array.isArray(json.files) ? json.files : [],\n};\n\nconst isSameEntry = (entry) =>\n  entry.revisionNumber === currentEntry.revisionNumber &&\n  entry.zipDownloadToken === currentEntry.zipDownloadToken &&\n  entry.zipDownloadUrl === currentEntry.zipDownloadUrl &&\n  entry.backendStructure === currentEntry.backendStructure;\n\nlet storedEntry = normalizedHistory.find(isSameEntry);\n\nconst usedBinaryPropertyNames = new Set(\n  normalizedHistory\n    .map((entry) => String(entry.binaryPropertyName ?? '').trim())\n    .filter(Boolean),\n);\n\nif (!storedEntry) {\n  const nextIterationNumber = normalizedHistory.reduce(\n    (maxValue, entry) => Math.max(maxValue, Number(entry.iterationNumber ?? 0) || 0),\n    0,\n  ) + 1;\n\n  let binaryPropertyName = '';\n  if (binary.backendZip) {\n    let suffix = nextIterationNumber;\n    do {\n      binaryPropertyName = `backend_iteration_${suffix}`;\n      suffix += 1;\n    } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n    binary[binaryPropertyName] = binary.backendZip;\n  }\n\n  storedEntry = {\n    iterationNumber: nextIterationNumber,\n    ...currentEntry,\n    binaryPropertyName,\n  };\n  normalizedHistory.push(storedEntry);\n} else if (!storedEntry.binaryPropertyName && binary.backendZip) {\n  let suffix = Number(storedEntry.iterationNumber ?? normalizedHistory.length + 1) || (normalizedHistory.length + 1);\n  let binaryPropertyName = '';\n  do {\n    binaryPropertyName = `backend_iteration_${suffix}`;\n    suffix += 1;\n  } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n  binary[binaryPropertyName] = binary.backendZip;\n  storedEntry.binaryPropertyName = binaryPropertyName;\n}\n\njson.backendHistory = normalizedHistory;\njson.currentBackendIterationNumber = Number(storedEntry.iterationNumber ?? 0) || 0;\n\nconst reviewAssets = buildBackendReviewAssets({\n  history: normalizedHistory,\n  currentIterationNumber: json.currentBackendIterationNumber,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: '',\n});\njson.backendReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.backendReviewFormJson = reviewAssets.formJson;\n\ndelete json.backendHistoryHtml;\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'backendZip' || /^backend_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        57840,
        12016
      ],
      "id": "3f7bc3de-3d36-4773-9d87-3ab984ff2cf1",
      "name": "BE - Store Iterations"
    },
    {
      "parameters": {
        "defineForm": "json",
        "jsonOutput": "={{ $json.backendReviewFormJson }}",
        "options": {
          "formTitle": "Human Review: Backend Draft",
          "formDescription": "={{ $json.backendReviewDescriptionHtml }}",
          "buttonLabel": "Submit"
        }
      },
      "id": "14026a2e-df4c-449b-9ab5-62a1882248be",
      "name": "BE - Review",
      "type": "n8n-nodes-base.form",
      "typeVersion": 2.5,
      "position": [
        58064,
        12080
      ],
      "webhookId": "9d6db7aa-72a2-4a75-9df1-fcd690176c4c"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "bb3442a6-db09-47c4-8f4b-1100d538530c",
      "name": "BE - Review Context",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        58272,
        12032
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "restore-older-api",
              "leftValue": "={{ $json[\"Restore older iteration\"] }}",
              "rightValue": "Yes",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "a4daab22-7d23-48cd-8ee8-a082c63970b6",
      "name": "BE - Restore Older Iteration?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        58480,
        12032
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\nconst history = Array.isArray(json.backendHistory)\n  ? json.backendHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildBackendReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Backend ZIP</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the backend package.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Backend ZIP, then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst selectionLabel = String(json['Iteration to restore'] ?? '').trim();\nconst selectionMatch = selectionLabel.match(/Iteration\\s+(\\d+)/i);\nconst selectedIterationNumber = selectionMatch ? Number.parseInt(selectionMatch[1], 10) : Number.NaN;\nconst currentIterationNumber = Number(json.currentBackendIterationNumber ?? 0) || 0;\nconst selectedEntry = history.find((entry) => Number(entry.iterationNumber ?? 0) === selectedIterationNumber);\n\nif (selectedEntry && Number(selectedEntry.iterationNumber ?? 0) !== currentIterationNumber) {\n  json.backendStructure = String(selectedEntry.backendStructure ?? selectedEntry.response ?? json.backendStructure ?? json.response ?? '').trim();\n  json.response = json.backendStructure;\n  json.fileCount = Number(selectedEntry.fileCount ?? json.fileCount ?? 0) || 0;\n  json.files = Array.isArray(selectedEntry.files) ? selectedEntry.files : (Array.isArray(json.files) ? json.files : []);\n  json.revisionNumber = Number(selectedEntry.revisionNumber ?? json.revisionNumber ?? 0) || 0;\n  json.zipDownloadUrl = String(selectedEntry.zipDownloadUrl ?? json.zipDownloadUrl ?? '').trim();\n  json.zipDownloadToken = String(selectedEntry.zipDownloadToken ?? json.zipDownloadToken ?? '').trim();\n  json.downloadLinkHtml = String(selectedEntry.downloadLinkHtml ?? json.downloadLinkHtml ?? '').trim();\n  json.currentBackendIterationNumber = Number(selectedEntry.iterationNumber ?? json.currentBackendIterationNumber ?? 0) || 0;\n  json.fileCount = Number(selectedEntry.fileCount ?? json.fileCount ?? 0) || 0;\n  json.files = Array.isArray(selectedEntry.files) ? selectedEntry.files : (Array.isArray(json.files) ? json.files : []);\n\n  const binaryPropertyName = String(selectedEntry.binaryPropertyName ?? '').trim();\n  if (binaryPropertyName && binary[binaryPropertyName]) {\n    binary.backendZip = binary[binaryPropertyName];\n  }\n\n  delete json.restoreSelectionError;\n} else {\n  json.restoreSelectionError = 'Select one of the previously generated iterations to restore it.';\n}\n\nconst reviewAssets = buildBackendReviewAssets({\n  history,\n  currentIterationNumber: Number(json.currentBackendIterationNumber ?? 0) || 0,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: String(json.restoreSelectionError ?? '').trim(),\n});\njson.backendReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.backendReviewFormJson = reviewAssets.formJson;\n\ndelete json.backendHistoryHtml;\ndelete json.reviewMode;\ndelete json.feedback;\ndelete json.Feedback;\ndelete json.Approved;\ndelete json.Attachments;\ndelete json['Restore older iteration'];\ndelete json['Iteration to restore'];\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'backendZip' || /^backend_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        58464,
        12272
      ],
      "id": "86fbe297-1bfd-4826-99b1-d4149bdf0336",
      "name": "BE - Restore Selected Iteration"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "approved-api",
              "leftValue": "={{ $json.Approved }}",
              "rightValue": "approved",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "0e481329-0321-4697-9823-e9a67131562f",
      "name": "BE - Approved?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        58688,
        12048
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nreturn [{\n  json: {\n    ...(item.json ?? {}),\n    reviewMode: 'refine',\n  },\n  ...(item.binary && Object.keys(item.binary).length ? { binary: item.binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        57648,
        12288
      ],
      "id": "50429a16-93fa-4b1d-89c2-204ddf87ccdd",
      "name": "BE - Prepare Refine Input"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst backendZip = item.binary?.backendZip;\n\nif (!backendZip) {\n  throw new Error('Approved backend ZIP is missing.');\n}\n\nreturn [{\n  json: {\n    revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n    fileCount: Number(json.fileCount ?? 0) || 0,\n    zipDownloadUrl: String(json.zipDownloadUrl ?? '').trim(),\n    zipDownloadToken: String(json.zipDownloadToken ?? '').trim(),\n    currentBackendIterationNumber: Number(json.currentBackendIterationNumber ?? 0) || 0,\n  },\n  binary: {\n    backendZip,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        58992,
        12032
      ],
      "id": "3b97b4d5-c143-4e17-a51c-1f292a0386b6",
      "name": "BE - End"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        58224,
        12544
      ],
      "id": "439c67d4-a9d6-43e0-9f6f-a025a8fc7188",
      "name": "Flow Node3"
    },
    {
      "parameters": {
        "content": "## Backend Generation Module\n\nWith the **structured requirements** and the **API specification** creates a **Backend Directory**.",
        "height": 672,
        "width": 2016,
        "color": "#FCE7F3"
      },
      "type": "n8n-nodes-base.stickyNote",
      "position": [
        57024,
        11824
      ],
      "typeVersion": 1,
      "id": "bc1bd955-c982-40b5-ad1d-6446e12fc2c4",
      "name": "Sticky Note3"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\n\nfor (const key of [\n  'design',\n  'finalDesign',\n  'finalDesignExcalidraw',\n  'firstIterationDesign',\n  'designDsl',\n  'firstIterationDesignDsl',\n  'finalDesignDsl',\n]) {\n  delete json[key];\n}\n\nconst binary = { ...(item.binary ?? {}) };\ndelete binary.design;\ndelete binary.design_final;\n\nreturn [{\n  json,\n  ...(Object.keys(binary).length ? { binary } : {}),\n}];"
      },
      "id": "56a41546-b0ef-403e-82e1-3b3764dcbbb4",
      "name": "BE - Strip Design Input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        57136,
        12016
      ]
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        59472,
        12528
      ],
      "id": "ab8147a8-fb75-44c4-ab80-a1c8ca21a6a5",
      "name": "FE - Start"
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "yN8cYN6dA9Ztvi4h",
          "mode": "list",
          "cachedResultUrl": "/workflow/yN8cYN6dA9Ztvi4h",
          "cachedResultName": "frontend-workflow"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {},
          "matchingColumns": [],
          "schema": [],
          "attemptToConvertTypes": false,
          "convertFieldsToString": true
        },
        "options": {}
      },
      "id": "829c4652-b8dd-43e5-975c-fe74b8828640",
      "name": "FE - Create Frontend",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        59872,
        12416
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = {};\nif (Array.isArray(item.json?.frontendHistory)) {\n  json.frontendHistory = item.json.frontendHistory;\n}\n\nconst binary = {};\nfor (const [name, value] of Object.entries(item.binary ?? {})) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'design' || name === 'frontendZip' || /^frontend_iteration_\\d+$/.test(name)) {\n    binary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(binary).length ? { binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        59872,
        12608
      ],
      "id": "bae90e59-aa80-450b-b7f7-a5d0989d4874",
      "name": "FE - History Context"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "3b253505-d4da-4ed3-a16e-2189222e3a51",
      "name": "FE - History Merge",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        60128,
        12512
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildFrontendReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Frontend ZIP</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the frontend package.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Frontend ZIP, then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst existingHistory = Array.isArray(json.frontendHistory)\n  ? json.frontendHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst normalizedHistory = existingHistory.map((entry, index) => ({\n  iterationNumber: Number(entry.iterationNumber ?? index + 1) || (index + 1),\n  revisionNumber: Number(entry.revisionNumber ?? 0) || 0,\n  frontendStructure: String(entry.frontendStructure ?? entry.response ?? '').trim(),\n  zipDownloadUrl: String(entry.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(entry.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(entry.downloadLinkHtml ?? '').trim(),\n  binaryPropertyName: String(entry.binaryPropertyName ?? '').trim(),\n  fileCount: Number(entry.fileCount ?? 0) || 0,\n  files: Array.isArray(entry.files) ? entry.files : [],\n}));\n\nconst currentEntry = {\n  revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n  frontendStructure: String(json.frontendStructure ?? json.response ?? '').trim(),\n  zipDownloadUrl: String(json.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(json.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(json.downloadLinkHtml ?? '').trim(),\n  fileCount: Number(json.fileCount ?? 0) || 0,\n  files: Array.isArray(json.files) ? json.files : [],\n};\n\nconst isSameEntry = (entry) =>\n  entry.revisionNumber === currentEntry.revisionNumber &&\n  entry.zipDownloadToken === currentEntry.zipDownloadToken &&\n  entry.zipDownloadUrl === currentEntry.zipDownloadUrl &&\n  entry.frontendStructure === currentEntry.frontendStructure;\n\nlet storedEntry = normalizedHistory.find(isSameEntry);\n\nconst usedBinaryPropertyNames = new Set(\n  normalizedHistory\n    .map((entry) => String(entry.binaryPropertyName ?? '').trim())\n    .filter(Boolean),\n);\n\nif (!storedEntry) {\n  const nextIterationNumber = normalizedHistory.reduce(\n    (maxValue, entry) => Math.max(maxValue, Number(entry.iterationNumber ?? 0) || 0),\n    0,\n  ) + 1;\n\n  let binaryPropertyName = '';\n  if (binary.frontendZip) {\n    let suffix = nextIterationNumber;\n    do {\n      binaryPropertyName = `frontend_iteration_${suffix}`;\n      suffix += 1;\n    } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n    binary[binaryPropertyName] = binary.frontendZip;\n  }\n\n  storedEntry = {\n    iterationNumber: nextIterationNumber,\n    ...currentEntry,\n    binaryPropertyName,\n  };\n  normalizedHistory.push(storedEntry);\n} else if (!storedEntry.binaryPropertyName && binary.frontendZip) {\n  let suffix = Number(storedEntry.iterationNumber ?? normalizedHistory.length + 1) || (normalizedHistory.length + 1);\n  let binaryPropertyName = '';\n  do {\n    binaryPropertyName = `frontend_iteration_${suffix}`;\n    suffix += 1;\n  } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n  binary[binaryPropertyName] = binary.frontendZip;\n  storedEntry.binaryPropertyName = binaryPropertyName;\n}\n\njson.frontendHistory = normalizedHistory;\njson.currentFrontendIterationNumber = Number(storedEntry.iterationNumber ?? 0) || 0;\n\nconst reviewAssets = buildFrontendReviewAssets({\n  history: normalizedHistory,\n  currentIterationNumber: json.currentFrontendIterationNumber,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: '',\n});\njson.frontendReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.frontendReviewFormJson = reviewAssets.formJson;\n\ndelete json.frontendHistoryHtml;\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'design' || name === 'frontendZip' || /^frontend_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        60336,
        12512
      ],
      "id": "48139e4f-b6ba-4482-8cf5-dd34239c029d",
      "name": "FE - Store Iterations"
    },
    {
      "parameters": {
        "defineForm": "json",
        "jsonOutput": "={{ $json.frontendReviewFormJson }}",
        "options": {
          "formTitle": "Human Review: Frontend Draft",
          "formDescription": "={{ $json.frontendReviewDescriptionHtml }}",
          "buttonLabel": "Submit"
        }
      },
      "id": "64453bf5-ba05-4fd2-8e66-6ce2b697228e",
      "name": "FE - Review",
      "type": "n8n-nodes-base.form",
      "typeVersion": 2.5,
      "position": [
        60560,
        12576
      ],
      "webhookId": "9d6db7aa-72a2-4a75-9df1-fcd690176c4c"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "912ecb73-a5e7-48e6-bac1-3e46eef42d53",
      "name": "FE - Review Context",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        60768,
        12528
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "restore-older-api",
              "leftValue": "={{ $json[\"Restore older iteration\"] }}",
              "rightValue": "Yes",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "a4256347-5a26-4cc8-a4f8-02806eef6415",
      "name": "FE - Restore Older Iteration?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        60976,
        12528
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\nconst history = Array.isArray(json.frontendHistory)\n  ? json.frontendHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildFrontendReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Frontend ZIP</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the frontend package.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Frontend ZIP, then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst selectionLabel = String(json['Iteration to restore'] ?? '').trim();\nconst selectionMatch = selectionLabel.match(/Iteration\\s+(\\d+)/i);\nconst selectedIterationNumber = selectionMatch ? Number.parseInt(selectionMatch[1], 10) : Number.NaN;\nconst currentIterationNumber = Number(json.currentFrontendIterationNumber ?? 0) || 0;\nconst selectedEntry = history.find((entry) => Number(entry.iterationNumber ?? 0) === selectedIterationNumber);\n\nif (selectedEntry && Number(selectedEntry.iterationNumber ?? 0) !== currentIterationNumber) {\n  json.frontendStructure = String(selectedEntry.frontendStructure ?? selectedEntry.response ?? json.frontendStructure ?? json.response ?? '').trim();\n  json.response = json.frontendStructure;\n  json.fileCount = Number(selectedEntry.fileCount ?? json.fileCount ?? 0) || 0;\n  json.files = Array.isArray(selectedEntry.files) ? selectedEntry.files : (Array.isArray(json.files) ? json.files : []);\n  json.revisionNumber = Number(selectedEntry.revisionNumber ?? json.revisionNumber ?? 0) || 0;\n  json.zipDownloadUrl = String(selectedEntry.zipDownloadUrl ?? json.zipDownloadUrl ?? '').trim();\n  json.zipDownloadToken = String(selectedEntry.zipDownloadToken ?? json.zipDownloadToken ?? '').trim();\n  json.downloadLinkHtml = String(selectedEntry.downloadLinkHtml ?? json.downloadLinkHtml ?? '').trim();\n  json.currentFrontendIterationNumber = Number(selectedEntry.iterationNumber ?? json.currentFrontendIterationNumber ?? 0) || 0;\n  json.fileCount = Number(selectedEntry.fileCount ?? json.fileCount ?? 0) || 0;\n  json.files = Array.isArray(selectedEntry.files) ? selectedEntry.files : (Array.isArray(json.files) ? json.files : []);\n\n  const binaryPropertyName = String(selectedEntry.binaryPropertyName ?? '').trim();\n  if (binaryPropertyName && binary[binaryPropertyName]) {\n    binary.frontendZip = binary[binaryPropertyName];\n  }\n\n  delete json.restoreSelectionError;\n} else {\n  json.restoreSelectionError = 'Select one of the previously generated iterations to restore it.';\n}\n\nconst reviewAssets = buildFrontendReviewAssets({\n  history,\n  currentIterationNumber: Number(json.currentFrontendIterationNumber ?? 0) || 0,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: String(json.restoreSelectionError ?? '').trim(),\n});\njson.frontendReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.frontendReviewFormJson = reviewAssets.formJson;\n\ndelete json.frontendHistoryHtml;\ndelete json.reviewMode;\ndelete json.feedback;\ndelete json.Feedback;\ndelete json.Approved;\ndelete json.Attachments;\ndelete json['Restore older iteration'];\ndelete json['Iteration to restore'];\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'design' || name === 'frontendZip' || /^frontend_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        60960,
        12768
      ],
      "id": "4cb4aa28-3454-470f-b272-e4043be0d822",
      "name": "FE - Restore Selected Iteration"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "approved-api",
              "leftValue": "={{ $json.Approved }}",
              "rightValue": "approved",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "02997e04-19ce-4a6f-993e-a2f22c07a65c",
      "name": "FE - Approved?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        61184,
        12544
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nreturn [{\n  json: {\n    ...(item.json ?? {}),\n    reviewMode: 'refine',\n  },\n  ...(item.binary && Object.keys(item.binary).length ? { binary: item.binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        60144,
        12784
      ],
      "id": "cb1844bc-fc26-4a37-a60b-cea61f8b5c28",
      "name": "FE - Prepare Refine Input"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst frontendZip = item.binary?.frontendZip;\n\nif (!frontendZip) {\n  throw new Error('Approved frontend ZIP is missing.');\n}\n\nreturn [{\n  json: {\n    revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n    fileCount: Number(json.fileCount ?? 0) || 0,\n    zipDownloadUrl: String(json.zipDownloadUrl ?? '').trim(),\n    zipDownloadToken: String(json.zipDownloadToken ?? '').trim(),\n    currentFrontendIterationNumber: Number(json.currentFrontendIterationNumber ?? 0) || 0,\n  },\n  binary: {\n    frontendZip,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        61488,
        12528
      ],
      "id": "417b6cc1-7e2b-421d-aafc-1feba6893247",
      "name": "FE - End"
    },
    {
      "parameters": {
        "content": "## Frontend Generation Module\n\nWith the **structured requirements**, the **API specification**, and the **compact design summary** creates a **Frontend Directory**.",
        "height": 720,
        "width": 2016,
        "color": "#E0F2F1"
      },
      "type": "n8n-nodes-base.stickyNote",
      "position": [
        59520,
        12320
      ],
      "typeVersion": 1,
      "id": "94b82110-5e89-4e1e-9dbd-09a9d1dd1a0b",
      "name": "Sticky Note4"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        59312,
        12032
      ],
      "id": "0c5387b0-f469-456b-8dce-758d9653fdd9",
      "name": "Flow Node4"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "df16b6d8-ebe8-4957-b910-47036572ebf0",
      "name": "FE - Input Merge",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        59200,
        12528
      ]
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        61952,
        13120
      ],
      "id": "5e606d45-6d44-4fff-baa0-95e14f6a0064",
      "name": "INF - Start"
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "DcoGbEIUb6nok9Y0",
          "mode": "list",
          "cachedResultUrl": "/workflow/DcoGbEIUb6nok9Y0",
          "cachedResultName": "infrastructure-workflow"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {},
          "matchingColumns": [],
          "schema": [],
          "attemptToConvertTypes": false,
          "convertFieldsToString": true
        },
        "options": {}
      },
      "id": "cb9dee36-82cf-4cc1-8b74-34aa545e399f",
      "name": "INF - Create Infrastructure",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        62352,
        13024
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = {};\nif (Array.isArray(item.json?.infrastructureHistory)) {\n  json.infrastructureHistory = item.json.infrastructureHistory;\n}\n\nconst binary = {};\nfor (const [name, value] of Object.entries(item.binary ?? {})) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'infrastructureZip' || /^infrastructure_iteration_\\d+$/.test(name)) {\n    binary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(binary).length ? { binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        62352,
        13216
      ],
      "id": "016e47f7-d80d-4e52-b5a5-59b7b211711e",
      "name": "INF - History Context"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "6965301c-5e97-40e6-88b2-4f015edc497d",
      "name": "INF - History Merge",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        62608,
        13120
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildInfrastructureReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Infrastructure ZIP</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the infrastructure package.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Infrastructure ZIP, then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst existingHistory = Array.isArray(json.infrastructureHistory)\n  ? json.infrastructureHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst normalizedHistory = existingHistory.map((entry, index) => ({\n  iterationNumber: Number(entry.iterationNumber ?? index + 1) || (index + 1),\n  revisionNumber: Number(entry.revisionNumber ?? 0) || 0,\n  infrastructureStructure: String(entry.infrastructureStructure ?? entry.response ?? '').trim(),\n  zipDownloadUrl: String(entry.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(entry.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(entry.downloadLinkHtml ?? '').trim(),\n  binaryPropertyName: String(entry.binaryPropertyName ?? '').trim(),\n  fileCount: Number(entry.fileCount ?? 0) || 0,\n  files: Array.isArray(entry.files) ? entry.files : [],\n}));\n\nconst currentEntry = {\n  revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n  infrastructureStructure: String(json.infrastructureStructure ?? json.response ?? '').trim(),\n  zipDownloadUrl: String(json.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(json.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(json.downloadLinkHtml ?? '').trim(),\n  fileCount: Number(json.fileCount ?? 0) || 0,\n  files: Array.isArray(json.files) ? json.files : [],\n};\n\nconst isSameEntry = (entry) =>\n  entry.revisionNumber === currentEntry.revisionNumber &&\n  entry.zipDownloadToken === currentEntry.zipDownloadToken &&\n  entry.zipDownloadUrl === currentEntry.zipDownloadUrl &&\n  entry.infrastructureStructure === currentEntry.infrastructureStructure;\n\nlet storedEntry = normalizedHistory.find(isSameEntry);\n\nconst usedBinaryPropertyNames = new Set(\n  normalizedHistory\n    .map((entry) => String(entry.binaryPropertyName ?? '').trim())\n    .filter(Boolean),\n);\n\nif (!storedEntry) {\n  const nextIterationNumber = normalizedHistory.reduce(\n    (maxValue, entry) => Math.max(maxValue, Number(entry.iterationNumber ?? 0) || 0),\n    0,\n  ) + 1;\n\n  let binaryPropertyName = '';\n  if (binary.infrastructureZip) {\n    let suffix = nextIterationNumber;\n    do {\n      binaryPropertyName = `infrastructure_iteration_${suffix}`;\n      suffix += 1;\n    } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n    binary[binaryPropertyName] = binary.infrastructureZip;\n  }\n\n  storedEntry = {\n    iterationNumber: nextIterationNumber,\n    ...currentEntry,\n    binaryPropertyName,\n  };\n  normalizedHistory.push(storedEntry);\n} else if (!storedEntry.binaryPropertyName && binary.infrastructureZip) {\n  let suffix = Number(storedEntry.iterationNumber ?? normalizedHistory.length + 1) || (normalizedHistory.length + 1);\n  let binaryPropertyName = '';\n  do {\n    binaryPropertyName = `infrastructure_iteration_${suffix}`;\n    suffix += 1;\n  } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n  binary[binaryPropertyName] = binary.infrastructureZip;\n  storedEntry.binaryPropertyName = binaryPropertyName;\n}\n\njson.infrastructureHistory = normalizedHistory;\njson.currentInfrastructureIterationNumber = Number(storedEntry.iterationNumber ?? 0) || 0;\n\nconst reviewAssets = buildInfrastructureReviewAssets({\n  history: normalizedHistory,\n  currentIterationNumber: json.currentInfrastructureIterationNumber,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: '',\n});\njson.infrastructureReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.infrastructureReviewFormJson = reviewAssets.formJson;\n\ndelete json.infrastructureHistoryHtml;\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'infrastructureZip' || /^infrastructure_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        62816,
        13120
      ],
      "id": "0ebd63e7-6da1-4623-b722-4890a7f516d3",
      "name": "INF - Store Iterations"
    },
    {
      "parameters": {
        "defineForm": "json",
        "jsonOutput": "={{ $json.infrastructureReviewFormJson }}",
        "options": {
          "formTitle": "Human Review: Infrastructure Draft",
          "formDescription": "={{ $json.infrastructureReviewDescriptionHtml }}",
          "buttonLabel": "Submit"
        }
      },
      "id": "ad5454cb-15c6-4ba2-b7ce-a1ea355648c7",
      "name": "INF - Review",
      "type": "n8n-nodes-base.form",
      "typeVersion": 2.5,
      "position": [
        63040,
        13184
      ],
      "webhookId": "9d6db7aa-72a2-4a75-9df1-fcd690176c4c"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "910f7f13-db37-4ff4-8b09-aacb18aa154a",
      "name": "INF - Review Context",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        63248,
        13136
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "restore-older-api",
              "leftValue": "={{ $json[\"Restore older iteration\"] }}",
              "rightValue": "Yes",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "f0d3485d-f3c4-4809-922b-8cf219db582c",
      "name": "INF - Restore Older Iteration?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        63456,
        13136
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\nconst history = Array.isArray(json.infrastructureHistory)\n  ? json.infrastructureHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildInfrastructureReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Infrastructure ZIP</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the infrastructure package.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Infrastructure ZIP, then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst selectionLabel = String(json['Iteration to restore'] ?? '').trim();\nconst selectionMatch = selectionLabel.match(/Iteration\\s+(\\d+)/i);\nconst selectedIterationNumber = selectionMatch ? Number.parseInt(selectionMatch[1], 10) : Number.NaN;\nconst currentIterationNumber = Number(json.currentInfrastructureIterationNumber ?? 0) || 0;\nconst selectedEntry = history.find((entry) => Number(entry.iterationNumber ?? 0) === selectedIterationNumber);\n\nif (selectedEntry && Number(selectedEntry.iterationNumber ?? 0) !== currentIterationNumber) {\n  json.infrastructureStructure = String(selectedEntry.infrastructureStructure ?? selectedEntry.response ?? json.infrastructureStructure ?? json.response ?? '').trim();\n  json.response = json.infrastructureStructure;\n  json.fileCount = Number(selectedEntry.fileCount ?? json.fileCount ?? 0) || 0;\n  json.files = Array.isArray(selectedEntry.files) ? selectedEntry.files : (Array.isArray(json.files) ? json.files : []);\n  json.revisionNumber = Number(selectedEntry.revisionNumber ?? json.revisionNumber ?? 0) || 0;\n  json.zipDownloadUrl = String(selectedEntry.zipDownloadUrl ?? json.zipDownloadUrl ?? '').trim();\n  json.zipDownloadToken = String(selectedEntry.zipDownloadToken ?? json.zipDownloadToken ?? '').trim();\n  json.downloadLinkHtml = String(selectedEntry.downloadLinkHtml ?? json.downloadLinkHtml ?? '').trim();\n  json.currentInfrastructureIterationNumber = Number(selectedEntry.iterationNumber ?? json.currentInfrastructureIterationNumber ?? 0) || 0;\n  json.fileCount = Number(selectedEntry.fileCount ?? json.fileCount ?? 0) || 0;\n  json.files = Array.isArray(selectedEntry.files) ? selectedEntry.files : (Array.isArray(json.files) ? json.files : []);\n\n  const binaryPropertyName = String(selectedEntry.binaryPropertyName ?? '').trim();\n  if (binaryPropertyName && binary[binaryPropertyName]) {\n    binary.infrastructureZip = binary[binaryPropertyName];\n  }\n\n  delete json.restoreSelectionError;\n} else {\n  json.restoreSelectionError = 'Select one of the previously generated iterations to restore it.';\n}\n\nconst reviewAssets = buildInfrastructureReviewAssets({\n  history,\n  currentIterationNumber: Number(json.currentInfrastructureIterationNumber ?? 0) || 0,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: String(json.restoreSelectionError ?? '').trim(),\n});\njson.infrastructureReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.infrastructureReviewFormJson = reviewAssets.formJson;\n\ndelete json.infrastructureHistoryHtml;\ndelete json.reviewMode;\ndelete json.feedback;\ndelete json.Feedback;\ndelete json.Approved;\ndelete json.Attachments;\ndelete json['Restore older iteration'];\ndelete json['Iteration to restore'];\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'infrastructureZip' || /^infrastructure_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        63440,
        13376
      ],
      "id": "0b3ba816-da08-46c1-94a6-bf8410b34243",
      "name": "INF - Restore Selected Iteration"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "approved-api",
              "leftValue": "={{ $json.Approved }}",
              "rightValue": "approved",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "51f6ec1f-e016-4183-ace3-5876a0888449",
      "name": "INF - Approved?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        63664,
        13152
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nreturn [{\n  json: {\n    ...(item.json ?? {}),\n    reviewMode: 'refine',\n  },\n  ...(item.binary && Object.keys(item.binary).length ? { binary: item.binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        62624,
        13392
      ],
      "id": "1aa411c1-494d-4074-a91e-098a9438270b",
      "name": "INF - Prepare Refine Input"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst infrastructureZip = item.binary?.infrastructureZip;\n\nif (!infrastructureZip) {\n  throw new Error('Approved infrastructure ZIP is missing.');\n}\n\nreturn [{\n  json: {\n    revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n    fileCount: Number(json.fileCount ?? 0) || 0,\n    zipDownloadUrl: String(json.zipDownloadUrl ?? '').trim(),\n    zipDownloadToken: String(json.zipDownloadToken ?? '').trim(),\n    currentInfrastructureIterationNumber: Number(json.currentInfrastructureIterationNumber ?? 0) || 0,\n  },\n  binary: {\n    infrastructureZip,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        63968,
        13136
      ],
      "id": "121f129f-0282-4c4f-b4e2-692c1060bd69",
      "name": "INF - End"
    },
    {
      "parameters": {
        "content": "## Infrastructure Generation Module\n\nWith the **structured requirements** and the **API specification** creates an **Infrastructure Directory**.",
        "height": 720,
        "width": 2016,
        "color": "#FFE8CC"
      },
      "type": "n8n-nodes-base.stickyNote",
      "position": [
        62000,
        12928
      ],
      "typeVersion": 1,
      "id": "d6acd03e-c378-4923-ba65-fafc764c6221",
      "name": "Sticky Note5"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        61808,
        12528
      ],
      "id": "d534d80c-10c5-4e2c-909e-76b379fe275e",
      "name": "Flow Node5"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        64384,
        14080
      ],
      "id": "14d7536d-849e-41cd-9f5f-71bc559ded5e",
      "name": "TEST - Start"
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "h7qFrKACAcRj8pcN",
          "mode": "list",
          "cachedResultUrl": "/workflow/h7qFrKACAcRj8pcN",
          "cachedResultName": "tests-workflow"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {},
          "matchingColumns": [],
          "schema": [],
          "attemptToConvertTypes": false,
          "convertFieldsToString": true
        },
        "options": {}
      },
      "id": "d3b3a5ab-419b-4c1a-afc9-3172595df7c1",
      "name": "TEST - Create Tests",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        64784,
        13984
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = {};\nif (Array.isArray(item.json?.testsHistory)) {\n  json.testsHistory = item.json.testsHistory;\n}\n\nconst binary = {};\nfor (const [name, value] of Object.entries(item.binary ?? {})) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'testsZip' || /^tests_iteration_\\d+$/.test(name)) {\n    binary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(binary).length ? { binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        64784,
        14176
      ],
      "id": "9e05e3a2-5122-4dfe-8380-085ebdb4ca27",
      "name": "TEST - History Context"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "8e418671-6be9-4a74-a47f-4f2d9c42e80c",
      "name": "TEST - History Merge",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        65040,
        14080
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildTestsReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Tests ZIP</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the tests package.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Tests ZIP, then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst existingHistory = Array.isArray(json.testsHistory)\n  ? json.testsHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst normalizedHistory = existingHistory.map((entry, index) => ({\n  iterationNumber: Number(entry.iterationNumber ?? index + 1) || (index + 1),\n  revisionNumber: Number(entry.revisionNumber ?? 0) || 0,\n  testsStructure: String(entry.testsStructure ?? entry.response ?? '').trim(),\n  zipDownloadUrl: String(entry.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(entry.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(entry.downloadLinkHtml ?? '').trim(),\n  binaryPropertyName: String(entry.binaryPropertyName ?? '').trim(),\n  fileCount: Number(entry.fileCount ?? 0) || 0,\n  files: Array.isArray(entry.files) ? entry.files : [],\n}));\n\nconst currentEntry = {\n  revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n  testsStructure: String(json.testsStructure ?? json.response ?? '').trim(),\n  zipDownloadUrl: String(json.zipDownloadUrl ?? '').trim(),\n  zipDownloadToken: String(json.zipDownloadToken ?? '').trim(),\n  downloadLinkHtml: String(json.downloadLinkHtml ?? '').trim(),\n  fileCount: Number(json.fileCount ?? 0) || 0,\n  files: Array.isArray(json.files) ? json.files : [],\n};\n\nconst isSameEntry = (entry) =>\n  entry.revisionNumber === currentEntry.revisionNumber &&\n  entry.zipDownloadToken === currentEntry.zipDownloadToken &&\n  entry.zipDownloadUrl === currentEntry.zipDownloadUrl &&\n  entry.testsStructure === currentEntry.testsStructure;\n\nlet storedEntry = normalizedHistory.find(isSameEntry);\n\nconst usedBinaryPropertyNames = new Set(\n  normalizedHistory\n    .map((entry) => String(entry.binaryPropertyName ?? '').trim())\n    .filter(Boolean),\n);\n\nif (!storedEntry) {\n  const nextIterationNumber = normalizedHistory.reduce(\n    (maxValue, entry) => Math.max(maxValue, Number(entry.iterationNumber ?? 0) || 0),\n    0,\n  ) + 1;\n\n  let binaryPropertyName = '';\n  if (binary.testsZip) {\n    let suffix = nextIterationNumber;\n    do {\n      binaryPropertyName = `tests_iteration_${suffix}`;\n      suffix += 1;\n    } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n    binary[binaryPropertyName] = binary.testsZip;\n  }\n\n  storedEntry = {\n    iterationNumber: nextIterationNumber,\n    ...currentEntry,\n    binaryPropertyName,\n  };\n  normalizedHistory.push(storedEntry);\n} else if (!storedEntry.binaryPropertyName && binary.testsZip) {\n  let suffix = Number(storedEntry.iterationNumber ?? normalizedHistory.length + 1) || (normalizedHistory.length + 1);\n  let binaryPropertyName = '';\n  do {\n    binaryPropertyName = `tests_iteration_${suffix}`;\n    suffix += 1;\n  } while (usedBinaryPropertyNames.has(binaryPropertyName) || binary[binaryPropertyName]);\n  binary[binaryPropertyName] = binary.testsZip;\n  storedEntry.binaryPropertyName = binaryPropertyName;\n}\n\njson.testsHistory = normalizedHistory;\njson.currentTestsIterationNumber = Number(storedEntry.iterationNumber ?? 0) || 0;\n\nconst reviewAssets = buildTestsReviewAssets({\n  history: normalizedHistory,\n  currentIterationNumber: json.currentTestsIterationNumber,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: '',\n});\njson.testsReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.testsReviewFormJson = reviewAssets.formJson;\n\ndelete json.testsHistoryHtml;\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'testsZip' || /^tests_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        65248,
        14080
      ],
      "id": "e5b3c6d5-301e-42ed-a63b-ba643cffa5ad",
      "name": "TEST - Store Iterations"
    },
    {
      "parameters": {
        "defineForm": "json",
        "jsonOutput": "={{ $json.testsReviewFormJson }}",
        "options": {
          "formTitle": "Human Review: Tests Draft",
          "formDescription": "={{ $json.testsReviewDescriptionHtml }}",
          "buttonLabel": "Submit"
        }
      },
      "id": "f7291186-1854-4828-9047-1c86edce6508",
      "name": "TEST - Review",
      "type": "n8n-nodes-base.form",
      "typeVersion": 2.5,
      "position": [
        65472,
        14144
      ],
      "webhookId": "9d6db7aa-72a2-4a75-9df1-fcd690176c4c"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "7ac974ef-3e98-4cfe-862c-5b2c41db1b66",
      "name": "TEST - Review Context",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        65680,
        14096
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "restore-older-api",
              "leftValue": "={{ $json[\"Restore older iteration\"] }}",
              "rightValue": "Yes",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "cf78f027-5bbc-4f7d-b7dc-20fdb4b8b625",
      "name": "TEST - Restore Older Iteration?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        65888,
        14096
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\nconst history = Array.isArray(json.testsHistory)\n  ? json.testsHistory.filter((entry) => entry && typeof entry === 'object')\n  : [];\n\nconst escapeHtml = (value) => String(value ?? '')\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\"/g, '&quot;')\n  .replace(/'/g, '&#39;');\n\nconst buildTestsReviewAssets = ({ history, currentIterationNumber, currentZipDownloadUrl, restoreError }) => {\n  const currentDownloadUrl = String(currentZipDownloadUrl ?? '').trim();\n  const currentDownloadHtml = currentDownloadUrl\n    ? `<p><a href=\"${escapeHtml(currentDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download current Tests ZIP</a></p>`\n    : '';\n\n  const historyItemsHtml = history.map((entry) => {\n    const iterationNumber = Number(entry.iterationNumber ?? 0) || 0;\n    const revisionNumber = Number(entry.revisionNumber ?? 0) || 0;\n    const currentTag = iterationNumber === currentIterationNumber ? ' <strong>(current)</strong>' : '';\n    const entryDownloadUrl = String(entry.zipDownloadUrl ?? '').trim();\n    const entryDownloadHtml = entryDownloadUrl\n      ? `<a href=\"${escapeHtml(entryDownloadUrl)}\" download target=\"_blank\" rel=\"noopener noreferrer\">Download ZIP</a>`\n      : '';\n    return `<li><strong>Iteration ${iterationNumber}</strong> - Revision ${escapeHtml(revisionNumber)}${currentTag}${entryDownloadHtml ? ` - ${entryDownloadHtml}` : ''}</li>`;\n  }).join('');\n\n  const restoreChoices = history\n    .filter((entry) => Number(entry.iterationNumber ?? 0) !== currentIterationNumber)\n    .map((entry) => ({\n      option: `Iteration ${Number(entry.iterationNumber ?? 0) || 0} (rev ${Number(entry.revisionNumber ?? 0) || 0})`,\n    }));\n\n  const formFields = [\n    {\n      fieldLabel: 'Approved',\n      fieldName: 'Approved',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'approved' },\n          { option: 'rejected' },\n        ],\n      },\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Restore older iteration',\n      fieldName: 'Restore older iteration',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: [\n          { option: 'Yes' },\n          { option: 'No' },\n        ],\n      },\n      defaultValue: 'No',\n      requiredField: true,\n    },\n    {\n      fieldLabel: 'Iteration to restore',\n      fieldName: 'Iteration to restore',\n      fieldType: 'dropdown',\n      fieldOptions: {\n        values: restoreChoices,\n      },\n    },\n    {\n      fieldLabel: 'Attachments (optional)',\n      fieldName: 'Attachments',\n      fieldType: 'file',\n    },\n    {\n      fieldLabel: 'Feedback',\n      fieldName: 'Feedback',\n      fieldType: 'textarea',\n      placeholder: 'Describe exactly what should change in the tests package.',\n    },\n  ];\n\n  const descriptionHtml = [\n    '<p>Review the current Tests ZIP, then approve it or request changes.</p>',\n    currentDownloadHtml,\n    restoreError ? `<p><strong>${escapeHtml(restoreError)}</strong></p>` : '',\n    '<p>If you want to return to a previously generated version, choose Yes in Restore older iteration. You can also download any generated iteration below.</p>',\n    historyItemsHtml ? `<p>Generated iterations:</p><ol>${historyItemsHtml}</ol>` : '<p>No generated iterations are available yet.</p>',\n  ].filter(Boolean).join('');\n\n  return {\n    descriptionHtml,\n    formJson: JSON.stringify(formFields),\n  };\n};\n\nconst selectionLabel = String(json['Iteration to restore'] ?? '').trim();\nconst selectionMatch = selectionLabel.match(/Iteration\\s+(\\d+)/i);\nconst selectedIterationNumber = selectionMatch ? Number.parseInt(selectionMatch[1], 10) : Number.NaN;\nconst currentIterationNumber = Number(json.currentTestsIterationNumber ?? 0) || 0;\nconst selectedEntry = history.find((entry) => Number(entry.iterationNumber ?? 0) === selectedIterationNumber);\n\nif (selectedEntry && Number(selectedEntry.iterationNumber ?? 0) !== currentIterationNumber) {\n  json.testsStructure = String(selectedEntry.testsStructure ?? selectedEntry.response ?? json.testsStructure ?? json.response ?? '').trim();\n  json.response = json.testsStructure;\n  json.fileCount = Number(selectedEntry.fileCount ?? json.fileCount ?? 0) || 0;\n  json.files = Array.isArray(selectedEntry.files) ? selectedEntry.files : (Array.isArray(json.files) ? json.files : []);\n  json.revisionNumber = Number(selectedEntry.revisionNumber ?? json.revisionNumber ?? 0) || 0;\n  json.zipDownloadUrl = String(selectedEntry.zipDownloadUrl ?? json.zipDownloadUrl ?? '').trim();\n  json.zipDownloadToken = String(selectedEntry.zipDownloadToken ?? json.zipDownloadToken ?? '').trim();\n  json.downloadLinkHtml = String(selectedEntry.downloadLinkHtml ?? json.downloadLinkHtml ?? '').trim();\n  json.currentTestsIterationNumber = Number(selectedEntry.iterationNumber ?? json.currentTestsIterationNumber ?? 0) || 0;\n  json.fileCount = Number(selectedEntry.fileCount ?? json.fileCount ?? 0) || 0;\n  json.files = Array.isArray(selectedEntry.files) ? selectedEntry.files : (Array.isArray(json.files) ? json.files : []);\n\n  const binaryPropertyName = String(selectedEntry.binaryPropertyName ?? '').trim();\n  if (binaryPropertyName && binary[binaryPropertyName]) {\n    binary.testsZip = binary[binaryPropertyName];\n  }\n\n  delete json.restoreSelectionError;\n} else {\n  json.restoreSelectionError = 'Select one of the previously generated iterations to restore it.';\n}\n\nconst reviewAssets = buildTestsReviewAssets({\n  history,\n  currentIterationNumber: Number(json.currentTestsIterationNumber ?? 0) || 0,\n  currentZipDownloadUrl: json.zipDownloadUrl,\n  restoreError: String(json.restoreSelectionError ?? '').trim(),\n});\njson.testsReviewDescriptionHtml = reviewAssets.descriptionHtml;\njson.testsReviewFormJson = reviewAssets.formJson;\n\ndelete json.testsHistoryHtml;\ndelete json.reviewMode;\ndelete json.feedback;\ndelete json.Feedback;\ndelete json.Approved;\ndelete json.Attachments;\ndelete json['Restore older iteration'];\ndelete json['Iteration to restore'];\n\nconst preservedBinary = {};\nfor (const [name, value] of Object.entries(binary)) {\n  if (name === 'requirements' || name === 'openapiSpec' || name === 'testsZip' || /^tests_iteration_\\d+$/.test(name)) {\n    preservedBinary[name] = value;\n  }\n}\n\nreturn [{\n  json,\n  ...(Object.keys(preservedBinary).length ? { binary: preservedBinary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        65872,
        14336
      ],
      "id": "0f760b4b-6a55-466a-b62d-ca02a902dbca",
      "name": "TEST - Restore Selected Iteration"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "approved-api",
              "leftValue": "={{ $json.Approved }}",
              "rightValue": "approved",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "4d07786d-085b-43c1-9ef6-0b44eab42c68",
      "name": "TEST - Approved?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        66096,
        14112
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nreturn [{\n  json: {\n    ...(item.json ?? {}),\n    reviewMode: 'refine',\n  },\n  ...(item.binary && Object.keys(item.binary).length ? { binary: item.binary } : {}),\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        65056,
        14352
      ],
      "id": "9571a2ef-48b6-4099-a306-7f8df24509dc",
      "name": "TEST - Prepare Refine Input"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst testsZip = item.binary?.testsZip;\n\nif (!testsZip) {\n  throw new Error('Approved tests ZIP is missing.');\n}\n\nreturn [{\n  json: {\n    revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n    fileCount: Number(json.fileCount ?? 0) || 0,\n    zipDownloadUrl: String(json.zipDownloadUrl ?? '').trim(),\n    zipDownloadToken: String(json.zipDownloadToken ?? '').trim(),\n    currentTestsIterationNumber: Number(json.currentTestsIterationNumber ?? 0) || 0,\n  },\n  binary: {\n    testsZip,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        66400,
        14096
      ],
      "id": "1fd22f16-5b8c-4c0b-91d9-9043e5804f36",
      "name": "TEST - End"
    },
    {
      "parameters": {
        "content": "## Test Generation Module\n\nWith the **structured requirements** and the **API specification** creates an **E2E Test Directory**.",
        "height": 720,
        "width": 2016,
        "color": "#FEF3C7"
      },
      "type": "n8n-nodes-base.stickyNote",
      "position": [
        64432,
        13888
      ],
      "typeVersion": 1,
      "id": "98f26969-5be9-4926-a8d0-ccff90674454",
      "name": "Sticky Note6"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        64352,
        13136
      ],
      "id": "65ec49de-460f-4c24-9291-1a9e0a622c86",
      "name": "Flow Node6"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "eba3ff8e-f475-40f2-8007-46c75b867b4a",
      "name": "INF - Input Merge",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        61744,
        13120
      ]
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "options": {}
      },
      "id": "b61608df-c2e6-42b5-9962-7390cc3adf77",
      "name": "TEST - Input Merge",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        64144,
        14080
      ]
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        58256,
        13152
      ],
      "id": "fdc0c166-28eb-45b1-a283-66266acdb448",
      "name": "Flow Node7"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        58256,
        14096
      ],
      "id": "26e09d1a-74b6-469a-a35d-4a982c6e7cf6",
      "name": "Flow Node8"
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "numberInputs": 5,
        "options": {}
      },
      "id": "3ae9901e-c602-4d8f-9c01-05272cabdece",
      "name": "FINAL - Merge Artifacts",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        59216,
        14368
      ]
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        59296,
        12288
      ],
      "id": "ec50314e-e526-459a-9f97-87ac8a44ffef",
      "name": "Flow Node9"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        61744,
        12928
      ],
      "id": "5f4cd6b7-1b45-4dc2-9e58-b0a2f35b65dc",
      "name": "Flow Node10"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        66624,
        14096
      ],
      "id": "2367f79a-2369-4c2b-b631-efea66bea06c",
      "name": "Flow Node11"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        66624,
        13632
      ],
      "id": "184a89d3-ae0c-4efc-a78b-9d89044d6e7a",
      "name": "Flow Node12"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        64144,
        13632
      ],
      "id": "f33e0b60-e5be-4419-920e-8cab84800295",
      "name": "Flow Node13"
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst inputBinary = { ...(item.binary ?? {}) };\nconst outputBinary = {};\n\nfunction cloneBinary(binary, fileName, fallbackMimeType) {\n  if (!binary) return null;\n  return {\n    ...binary,\n    fileName,\n    mimeType: String(binary.mimeType ?? fallbackMimeType ?? '').trim() || fallbackMimeType,\n  };\n}\n\nfunction addTextBinary(propertyName, fileName, text, mimeType) {\n  const value = String(text ?? '').trim();\n  if (!value) return false;\n  outputBinary[propertyName] = {\n    data: Buffer.from(value, 'utf8').toString('base64'),\n    fileName,\n    mimeType,\n  };\n  return true;\n}\n\nconst requirementsBinary = cloneBinary(inputBinary.requirements, 'requirements/requirements.md', 'text/markdown')\n  ?? (addTextBinary('bundleRequirements', 'requirements/requirements.md', json.finalRequirements ?? '', 'text/markdown') ? outputBinary.bundleRequirements : null);\nif (requirementsBinary && !outputBinary.bundleRequirements) outputBinary.bundleRequirements = requirementsBinary;\n\nconst designBinary = cloneBinary(inputBinary.design, 'design/design.excalidraw.json', 'application/json')\n  ?? (addTextBinary('bundleDesign', 'design/design.excalidraw.json', json.finalDesignExcalidraw ?? json.finalDesign ?? '', 'application/json') ? outputBinary.bundleDesign : null);\nif (designBinary && !outputBinary.bundleDesign) outputBinary.bundleDesign = designBinary;\n\nconst apiBinary = cloneBinary((inputBinary.api ?? inputBinary.openapiSpec), 'api/openapi.yaml', 'application/yaml')\n  ?? (addTextBinary('bundleApi', 'api/openapi.yaml', json.finalOpenapiYaml ?? '', 'application/yaml') ? outputBinary.bundleApi : null);\nif (apiBinary && !outputBinary.bundleApi) outputBinary.bundleApi = apiBinary;\n\nconst required = ['bundleRequirements', 'bundleDesign', 'bundleApi'];\nconst missing = required.filter((property) => !outputBinary[property]);\nif (missing.length) {\n  throw new Error('Missing required base artifacts: ' + missing.join(', '));\n}\n\nreturn [{\n  json,\n  binary: outputBinary,\n}];"
      },
      "id": "0f65b8c4-dc85-487c-b87e-10533bf8d3df",
      "name": "FINAL - Prepare Static Artifacts",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        60032,
        14416
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\n\nconst moduleFields = ['backendZip', 'frontendZip', 'infrastructureZip', 'testsZip'];\nfor (const field of moduleFields) {\n  const file = binary[field];\n  if (!file) {\n    throw new Error('Missing uploaded ZIP field: ' + field);\n  }\n  const fileName = String(file.fileName ?? `${field}.zip`).replace(/\\\\/g, '/');\n  binary[field] = {\n    ...file,\n    fileName,\n    mimeType: 'application/zip',\n    fileExtension: 'zip',\n  };\n}\n\nreturn [{ json, binary }];"
      },
      "id": "90495cbd-ef36-4570-8f2b-58807fb4c66d",
      "name": "FINAL - Prepare Module ZIP Metadata",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        59488,
        14864
      ]
    },
    {
      "parameters": {
        "binaryPropertyName": "backendZip",
        "outputPrefix": "be_"
      },
      "id": "148c03cc-d927-4f7b-a6e2-17522f60776e",
      "name": "FINAL - Unpack Backend ZIP",
      "type": "n8n-nodes-base.compression",
      "typeVersion": 1.1,
      "position": [
        59776,
        14592
      ]
    },
    {
      "parameters": {
        "binaryPropertyName": "frontendZip",
        "outputPrefix": "fe_"
      },
      "id": "7d0b508d-3cb1-4c2b-854b-71e27129e346",
      "name": "FINAL - Unpack Frontend ZIP",
      "type": "n8n-nodes-base.compression",
      "typeVersion": 1.1,
      "position": [
        59776,
        14768
      ]
    },
    {
      "parameters": {
        "binaryPropertyName": "infrastructureZip",
        "outputPrefix": "in_"
      },
      "id": "3701c131-7b12-4aea-a768-55e7d77fa0ad",
      "name": "FINAL - Unpack Infrastructure ZIP",
      "type": "n8n-nodes-base.compression",
      "typeVersion": 1.1,
      "position": [
        59776,
        14944
      ]
    },
    {
      "parameters": {
        "binaryPropertyName": "testsZip",
        "outputPrefix": "te_"
      },
      "id": "a9a057b1-eea4-43b0-9123-9f533157201f",
      "name": "FINAL - Unpack Tests ZIP",
      "type": "n8n-nodes-base.compression",
      "typeVersion": 1.1,
      "position": [
        59776,
        15120
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst inputBinary = { ...(item.binary ?? {}) };\nconst outputBinary = {};\n\nconst moduleName = 'backend';\n\nfor (const [key, entry] of Object.entries(inputBinary)) {\n  if (!entry || typeof entry !== 'object') continue;\n\n  const directory = String(entry.directory ?? '')\n    .replace(/\\\\/g, '/')\n    .replace(/^\\/+|\\/+$/g, '');\n  const fileName = String(entry.fileName ?? '')\n    .replace(/\\\\/g, '/')\n    .replace(/^\\/+/, '');\n\n  if (!fileName || fileName.endsWith('/')) continue;\n\n  const extractedPath = [directory, fileName].filter(Boolean).join('/').replace(/\\/+/g, '/');\n  if (!extractedPath || extractedPath.endsWith('/')) continue;\n\n  const modulePrefix = moduleName.toLowerCase() + '/';\n  const normalizedPath = extractedPath.toLowerCase().startsWith(modulePrefix)\n    ? extractedPath\n    : moduleName + '/' + extractedPath;\n\n  // Force compression to use this exact relative path.\n  const { directory: _ignoredDirectory, ...rest } = entry;\n  outputBinary[moduleName + '__' + key] = {\n    ...rest,\n    fileName: normalizedPath,\n  };\n}\n\nif (!Object.keys(outputBinary).length) {\n  throw new Error('No files were extracted for module: ' + moduleName);\n}\n\nreturn [{ json, binary: outputBinary }];"
      },
      "id": "6bf0ccf1-18f2-454c-937a-b2b1bff43294",
      "name": "FINAL - Normalize Backend Files",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        60032,
        14592
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst inputBinary = { ...(item.binary ?? {}) };\nconst outputBinary = {};\n\nconst moduleName = 'frontend';\n\nfor (const [key, entry] of Object.entries(inputBinary)) {\n  if (!entry || typeof entry !== 'object') continue;\n\n  const directory = String(entry.directory ?? '')\n    .replace(/\\\\/g, '/')\n    .replace(/^\\/+|\\/+$/g, '');\n  const fileName = String(entry.fileName ?? '')\n    .replace(/\\\\/g, '/')\n    .replace(/^\\/+/, '');\n\n  if (!fileName || fileName.endsWith('/')) continue;\n\n  const extractedPath = [directory, fileName].filter(Boolean).join('/').replace(/\\/+/g, '/');\n  if (!extractedPath || extractedPath.endsWith('/')) continue;\n\n  const modulePrefix = moduleName.toLowerCase() + '/';\n  const normalizedPath = extractedPath.toLowerCase().startsWith(modulePrefix)\n    ? extractedPath\n    : moduleName + '/' + extractedPath;\n\n  // Force compression to use this exact relative path.\n  const { directory: _ignoredDirectory, ...rest } = entry;\n  outputBinary[moduleName + '__' + key] = {\n    ...rest,\n    fileName: normalizedPath,\n  };\n}\n\nif (!Object.keys(outputBinary).length) {\n  throw new Error('No files were extracted for module: ' + moduleName);\n}\n\nreturn [{ json, binary: outputBinary }];"
      },
      "id": "5e5f6b64-dfd4-44c2-85da-5298790eacbf",
      "name": "FINAL - Normalize Frontend Files",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        60032,
        14768
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst inputBinary = { ...(item.binary ?? {}) };\nconst outputBinary = {};\n\nconst moduleName = 'infrastructure';\n\nfor (const [key, entry] of Object.entries(inputBinary)) {\n  if (!entry || typeof entry !== 'object') continue;\n\n  const directory = String(entry.directory ?? '')\n    .replace(/\\\\/g, '/')\n    .replace(/^\\/+|\\/+$/g, '');\n  const fileName = String(entry.fileName ?? '')\n    .replace(/\\\\/g, '/')\n    .replace(/^\\/+/, '');\n\n  if (!fileName || fileName.endsWith('/')) continue;\n\n  const extractedPath = [directory, fileName].filter(Boolean).join('/').replace(/\\/+/g, '/');\n  if (!extractedPath || extractedPath.endsWith('/')) continue;\n\n  const modulePrefix = moduleName.toLowerCase() + '/';\n  const normalizedPath = extractedPath.toLowerCase().startsWith(modulePrefix)\n    ? extractedPath\n    : moduleName + '/' + extractedPath;\n\n  // Force compression to use this exact relative path.\n  const { directory: _ignoredDirectory, ...rest } = entry;\n  outputBinary[moduleName + '__' + key] = {\n    ...rest,\n    fileName: normalizedPath,\n  };\n}\n\nif (!Object.keys(outputBinary).length) {\n  throw new Error('No files were extracted for module: ' + moduleName);\n}\n\nreturn [{ json, binary: outputBinary }];"
      },
      "id": "7e12a3f2-d32f-402c-bb0b-c916be1e553f",
      "name": "FINAL - Normalize Infrastructure Files",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        60032,
        14944
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst inputBinary = { ...(item.binary ?? {}) };\nconst outputBinary = {};\n\nconst moduleName = 'tests';\n\nfor (const [key, entry] of Object.entries(inputBinary)) {\n  if (!entry || typeof entry !== 'object') continue;\n\n  const directory = String(entry.directory ?? '')\n    .replace(/\\\\/g, '/')\n    .replace(/^\\/+|\\/+$/g, '');\n  const fileName = String(entry.fileName ?? '')\n    .replace(/\\\\/g, '/')\n    .replace(/^\\/+/, '');\n\n  if (!fileName || fileName.endsWith('/')) continue;\n\n  const extractedPath = [directory, fileName].filter(Boolean).join('/').replace(/\\/+/g, '/');\n  if (!extractedPath || extractedPath.endsWith('/')) continue;\n\n  const modulePrefix = moduleName.toLowerCase() + '/';\n  const normalizedPath = extractedPath.toLowerCase().startsWith(modulePrefix)\n    ? extractedPath\n    : moduleName + '/' + extractedPath;\n\n  // Force compression to use this exact relative path.\n  const { directory: _ignoredDirectory, ...rest } = entry;\n  outputBinary[moduleName + '__' + key] = {\n    ...rest,\n    fileName: normalizedPath,\n  };\n}\n\nif (!Object.keys(outputBinary).length) {\n  throw new Error('No files were extracted for module: ' + moduleName);\n}\n\nreturn [{ json, binary: outputBinary }];"
      },
      "id": "db2adf34-fb6a-437d-ae55-92bb1aedd2e3",
      "name": "FINAL - Normalize Tests Files",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        60032,
        15120
      ]
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "numberInputs": 5,
        "options": {}
      },
      "id": "fd31d162-e760-4e14-91fe-3cbf5ae787c9",
      "name": "FINAL - Merge Bundle Parts",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        60368,
        14720
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst binary = { ...(item.binary ?? {}) };\n\nif (!Object.keys(binary).length) {\n  throw new Error('No bundle files were prepared.');\n}\n\nconst requiredPaths = [\n  'requirements/requirements.md',\n  'design/design.excalidraw.json',\n  'api/openapi.yaml',\n];\nconst fileNames = Object.values(binary).map((entry) => String(entry?.fileName ?? '').replace(/\\\\/g, '/'));\nfor (const requiredPath of requiredPaths) {\n  if (!fileNames.includes(requiredPath)) {\n    throw new Error('Missing required bundle file: ' + requiredPath);\n  }\n}\n\nconst nestedZipMarkers = fileNames.filter((name) =>\n  /^(backend|frontend|infrastructure|tests)\\/.+\\.zip$/i.test(name)\n);\nif (nestedZipMarkers.length) {\n  throw new Error('Nested zip files detected inside module folders: ' + nestedZipMarkers.join(', '));\n}\n\nconst now = new Date();\nconst stamp = now.toISOString().replace(/[:.]/g, '-');\nconst fileName = `application-generation-${stamp}.zip`;\n\nreturn [{\n  json: {\n    ...json,\n    finalBundleFileName: fileName,\n    finalBundleCreatedAt: now.toISOString(),\n    finalBundleFolders: ['requirements', 'design', 'api', 'backend', 'frontend', 'infrastructure', 'tests'],\n  },\n  binary,\n}];"
      },
      "id": "49eb5de1-3289-4dac-a898-8327a20c5397",
      "name": "FINAL - Build Bundle Files",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        60592,
        14768
      ]
    },
    {
      "parameters": {
        "operation": "compress",
        "binaryPropertyName": "={{ Object.keys($binary).join(\",\") }}",
        "fileName": "={{ $json.finalBundleFileName || \"application-generation.zip\" }}"
      },
      "id": "0220a673-4d0a-40aa-9a41-6eaf7f6ab313",
      "name": "FINAL - Create Master ZIP",
      "type": "n8n-nodes-base.compression",
      "typeVersion": 1.1,
      "position": [
        60864,
        14768
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = item.json ?? {};\nconst binary = item.binary ?? {};\nconst zipBinary = binary.masterBundle ?? binary.data;\n\nif (!zipBinary) {\n  throw new Error('Final ZIP binary is required before Create ZIP Link.');\n}\n\nconst archiveData = String(zipBinary.data ?? (zipBinary.id ? 'filesystem-v2' : '')).trim();\nconst archiveId = String(zipBinary.id ?? '').trim();\nconst archiveMimeType = String(zipBinary.mimeType ?? 'application/zip').trim() || 'application/zip';\nconst archiveFileName = String(zipBinary.fileName ?? json.finalBundleFileName ?? 'application-generation.zip').trim() || 'application-generation.zip';\n\nif (!archiveData) {\n  throw new Error('Final ZIP binary payload is empty.');\n}\nif (archiveData === 'filesystem-v2' && !archiveId) {\n  throw new Error('Final ZIP id is required for filesystem-backed binary payload.');\n}\n\nreturn [{\n  json: {\n    ...json,\n    archiveData,\n    archiveId,\n    archiveMimeType,\n    archiveFileName,\n    archiveBinaryProperty: 'data',\n    response: String(json.response ?? 'All module artifacts packed into a final bundle.'),\n    revisionNumber: Number(json.revisionNumber ?? 0) || 0,\n  },\n  binary,\n}];"
      },
      "id": "a4245e75-3085-4c36-b05e-f71c0a79c2ba",
      "name": "FINAL - Prepare Zip Link Input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        61136,
        14768
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first() ?? { json: {}, binary: {} };\nconst json = { ...(item.json ?? {}) };\nconst url = String(json.zipDownloadUrl ?? '/webhook/review-download-file').trim() || '/webhook/review-download-file';\nconst safeUrl = url\n  .replace(/&/g, '&amp;')\n  .replace(/\"/g, '&quot;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;');\n\nconst completionHtml = `\n<div style=\"position:fixed;inset:0;display:flex;align-items:center;justify-content:center;padding:20px;background:radial-gradient(circle at 20% 0%,#eef5ff 0%,#e6eefb 48%,#dce6f5 100%);font-family:'Segoe UI',Tahoma,Arial,sans-serif;overflow:auto;\">\n  <div style=\"width:min(560px,95vw);background:#ffffffd9;border:1px solid #d9e4f2;border-radius:14px;box-shadow:0 12px 30px rgba(28,64,109,0.13);padding:24px 22px 22px;position:relative;\">\n    <div style=\"width:92px;height:92px;margin:-68px auto 10px;border-radius:999px;background:linear-gradient(140deg,#88d679 0%,#31b2cd 100%);display:flex;align-items:center;justify-content:center;color:#fff;font-size:48px;border:7px solid #edf5ff;box-shadow:0 8px 20px rgba(37,128,176,0.32);\">&#10003;</div>\n    <h1 style=\"margin:0 0 10px;text-align:center;color:#273d69;font-size:29px;line-height:1.12;font-weight:800;\">Application Generated!</h1>\n    <p style=\"margin:0 0 18px;text-align:center;color:#6c7d97;font-size:15px;line-height:1.45;\">Your application has been generated successfully. Click the button below to download the generated ZIP file.</p>\n\n    <div style=\"display:flex;align-items:center;gap:10px;border:1px solid #d8e3f2;border-radius:11px;background:#f8fbff;padding:10px 11px;box-shadow:0 2px 10px rgba(29,84,142,0.07);\">\n      <div style=\"width:46px;height:46px;border-radius:9px;background:linear-gradient(180deg,#5f9bf6 0%,#2f6fda 100%);display:flex;align-items:center;justify-content:center;color:#fff;font-size:16px;font-weight:800;\">ZIP</div>\n      <div style=\"flex:1;min-width:0;\">\n        <div style=\"font-size:13px;font-weight:700;color:#375382;margin-bottom:3px;\">Download your ZIP file here:</div>\n        <a href=\"${safeUrl}\" style=\"display:inline-block;max-width:100%;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:#2f63c5;text-decoration:none;font-size:14px;line-height:1.3;font-weight:600;\">application_generated.zip</a>\n      </div>\n      <a href=\"${safeUrl}\" style=\"display:inline-flex;align-items:center;justify-content:center;width:36px;height:36px;border-radius:8px;background:linear-gradient(180deg,#5f9cf7 0%,#2f70da 100%);color:#fff;text-decoration:none;font-size:18px;\">&#11015;</a>\n    </div>\n\n    <div style=\"margin-top:20px;padding-top:18px;border-top:1px solid #e6edf8;text-align:center;\">\n      <button type=\"button\" onclick=\"try{window.open('','_self');window.close();}catch(e){} setTimeout(function(){if(!window.closed){window.location.replace('about:blank');}},60);\" style=\"display:inline-block;padding:9px 34px;border:0;border-radius:9px;background:linear-gradient(180deg,#5ea1ff 0%,#2f6fda 100%);color:#fff;font-size:18px;font-weight:700;line-height:1;cursor:pointer;\">Finish</button>\n    </div>\n  </div>\n</div>`;\n\nreturn [{\n  json: {\n    ...json,\n    completionHtml,\n  },\n  binary: item.binary ?? {},\n}];"
      },
      "id": "1c419f50-70cf-4f16-bc17-79422e21f333",
      "name": "FINAL - Prepare Completion Page",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        61680,
        14768
      ]
    },
    {
      "parameters": {
        "operation": "completion",
        "respondWith": "showText",
        "responseText": "={{ $json.completionHtml }}"
      },
      "id": "f5704a5d-25b2-49f1-a1a5-b74d6e1d1c67",
      "name": "FINAL - Done",
      "type": "n8n-nodes-base.form",
      "typeVersion": 2.5,
      "position": [
        61952,
        14768
      ],
      "webhookId": "21644fe1-a7ee-441d-aa9e-645c0a286c7b"
    },
    {
      "parameters": {
        "content": "## Final Delivery\n\nCollects all approved artifacts, generates the final ZIP, and shows the completion download screen.",
        "height": 1072,
        "width": 2752,
        "color": "#FDE2E4"
      },
      "type": "n8n-nodes-base.stickyNote",
      "position": [
        59248,
        14240
      ],
      "typeVersion": 1,
      "id": "09eca724-0cc2-4bdf-a4d4-60144f2738b5",
      "name": "Sticky Note7"
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "Se3rPyRTvgimNBXz",
          "mode": "list",
          "cachedResultUrl": "/workflow/Se3rPyRTvgimNBXz",
          "cachedResultName": "Subworkflow - Create Link"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {
            "revisionNumber": "={{ $json.revisionNumber || 0 }}",
            "archiveMimeType": "={{ $json.archiveMimeType }}",
            "archiveFileName": "={{ $json.archiveFileName }}",
            "archiveData": "={{ $json.archiveData }}",
            "archiveId": "={{ $json.archiveId }}",
            "archiveBinaryProperty": "={{ $json.archiveBinaryProperty || \"data\" }}",
            "response": "={{ $json.response }}"
          },
          "matchingColumns": [],
          "schema": [
            {
              "id": "archiveMimeType",
              "displayName": "archiveMimeType",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "canBeUsedToMatch": true,
              "type": "string"
            },
            {
              "id": "archiveFileName",
              "displayName": "archiveFileName",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "canBeUsedToMatch": true,
              "type": "string"
            },
            {
              "id": "archiveData",
              "displayName": "archiveData",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "canBeUsedToMatch": true,
              "type": "string"
            },
            {
              "id": "archiveId",
              "displayName": "archiveId",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "canBeUsedToMatch": true,
              "type": "string"
            },
            {
              "id": "archiveBinaryProperty",
              "displayName": "archiveBinaryProperty",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "canBeUsedToMatch": true,
              "type": "string"
            },
            {
              "id": "revisionNumber",
              "displayName": "revisionNumber",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "canBeUsedToMatch": true,
              "type": "number"
            },
            {
              "id": "response",
              "displayName": "response",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "canBeUsedToMatch": true,
              "type": "string"
            }
          ],
          "attemptToConvertTypes": false,
          "convertFieldsToString": true
        },
        "options": {}
      },
      "id": "76285d7f-4238-40fe-945e-ba911aadc770",
      "name": "FINAL - Create ZIP Link",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        61424,
        14768
      ]
    }
  ],
  "pinData": {},
  "connections": {
    "DESIGN - Restore Older Iteration?": {
      "main": [
        [
          {
            "node": "DESIGN - Restore Selected Iteration",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "DESIGN - Approved?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "DESIGN - Approved?": {
      "main": [
        [
          {
            "node": "DESIGN - End",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "DESIGN - Prepare Refine Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "DESIGN - History Context": {
      "main": [
        [
          {
            "node": "DESIGN - History Merge",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "DESIGN - History Merge": {
      "main": [
        [
          {
            "node": "DESIGN - Store Iterations",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "DESIGN - Store Iterations": {
      "main": [
        [
          {
            "node": "DESIGN - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "DESIGN - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "DESIGN - Review": {
      "main": [
        [
          {
            "node": "DESIGN - Review Context",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "DESIGN - Review Context": {
      "main": [
        [
          {
            "node": "DESIGN - Restore Older Iteration?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "DESIGN - Prepare Refine Input": {
      "main": [
        [
          {
            "node": "DESIGN - History Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "DESIGN - Create Design",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "DESIGN - Restore Selected Iteration": {
      "main": [
        [
          {
            "node": "DESIGN - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "DESIGN - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "DESIGN - Start": {
      "main": [
        [
          {
            "node": "DESIGN - Create Design",
            "type": "main",
            "index": 0
          },
          {
            "node": "DESIGN - History Context",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "REQ - Approved?": {
      "main": [
        [
          {
            "node": "REQ - End",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "REQ - Prepare Refine Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "REQ - Restore Older Iteration?": {
      "main": [
        [
          {
            "node": "REQ - Restore Selected Iteration",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "REQ - Approved?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "REQ - Restore Selected Iteration": {
      "main": [
        [
          {
            "node": "REQ - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "REQ - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "REQ - Review Context": {
      "main": [
        [
          {
            "node": "REQ - Restore Older Iteration?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "REQ - Store Iterations": {
      "main": [
        [
          {
            "node": "REQ - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "REQ - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "REQ - Review": {
      "main": [
        [
          {
            "node": "REQ - Review Context",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "REQ - History Merge": {
      "main": [
        [
          {
            "node": "REQ - Store Iterations",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "REQ - Prepare Refine Input": {
      "main": [
        [
          {
            "node": "REQ - History Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "REQ - Create Requirements",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "REQ - History Context": {
      "main": [
        [
          {
            "node": "REQ - History Merge",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "REQ - Create Requirements": {
      "main": [
        [
          {
            "node": "REQ - History Merge",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "REQ - Start": {
      "main": [
        [
          {
            "node": "REQ - History Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "REQ - Create Requirements",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Start": {
      "main": [
        [
          {
            "node": "REQ - Start",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "DESIGN - Create Design": {
      "main": [
        [
          {
            "node": "DESIGN - History Merge",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Flow Node1": {
      "main": [
        [
          {
            "node": "DESIGN - Start",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "API - Start": {
      "main": [
        [
          {
            "node": "API - Create API",
            "type": "main",
            "index": 0
          },
          {
            "node": "API - History Context",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "API - Create API": {
      "main": [
        [
          {
            "node": "API - History Merge",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "API - History Context": {
      "main": [
        [
          {
            "node": "API - History Merge",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "API - History Merge": {
      "main": [
        [
          {
            "node": "API - Store Iterations",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "API - Store Iterations": {
      "main": [
        [
          {
            "node": "API - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "API - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "API - Review": {
      "main": [
        [
          {
            "node": "API - Review Context",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "API - Review Context": {
      "main": [
        [
          {
            "node": "API - Restore Older Iteration?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "API - Restore Older Iteration?": {
      "main": [
        [
          {
            "node": "API - Restore Selected Iteration",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "API - Approved?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "API - Restore Selected Iteration": {
      "main": [
        [
          {
            "node": "API - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "API - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "API - Approved?": {
      "main": [
        [
          {
            "node": "API - End",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "API - Prepare Refine Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "API - Prepare Refine Input": {
      "main": [
        [
          {
            "node": "API - History Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "API - Create API",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "DESIGN - End": {
      "main": [
        [
          {
            "node": "Flow Node2",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Flow Node2": {
      "main": [
        [
          {
            "node": "API - Start",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "REQ - End": {
      "main": [
        [
          {
            "node": "Flow Node1",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "API - End": {
      "main": [
        [
          {
            "node": "Flow Node3",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "BE - Start": {
      "main": [
        [
          {
            "node": "BE - Strip Design Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "BE - Create Backend": {
      "main": [
        [
          {
            "node": "BE - History Merge",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "BE - History Context": {
      "main": [
        [
          {
            "node": "BE - History Merge",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "BE - History Merge": {
      "main": [
        [
          {
            "node": "BE - Store Iterations",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "BE - Store Iterations": {
      "main": [
        [
          {
            "node": "BE - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "BE - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "BE - Review": {
      "main": [
        [
          {
            "node": "BE - Review Context",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "BE - Review Context": {
      "main": [
        [
          {
            "node": "BE - Restore Older Iteration?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "BE - Restore Older Iteration?": {
      "main": [
        [
          {
            "node": "BE - Restore Selected Iteration",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "BE - Approved?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "BE - Restore Selected Iteration": {
      "main": [
        [
          {
            "node": "BE - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "BE - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "BE - Approved?": {
      "main": [
        [
          {
            "node": "BE - End",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "BE - Prepare Refine Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "BE - Prepare Refine Input": {
      "main": [
        [
          {
            "node": "BE - History Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "BE - Create Backend",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "BE - Strip Design Input": {
      "main": [
        [
          {
            "node": "BE - Create Backend",
            "type": "main",
            "index": 0
          },
          {
            "node": "BE - History Context",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "BE - End": {
      "main": [
        [
          {
            "node": "Flow Node4",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Flow Node4": {
      "main": [
        [
          {
            "node": "FE - Input Merge",
            "type": "main",
            "index": 0
          },
          {
            "node": "Flow Node9",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FE - Input Merge": {
      "main": [
        [
          {
            "node": "FE - Start",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FE - Start": {
      "main": [
        [
          {
            "node": "FE - Create Frontend",
            "type": "main",
            "index": 0
          },
          {
            "node": "FE - History Context",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FE - Create Frontend": {
      "main": [
        [
          {
            "node": "FE - History Merge",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FE - History Context": {
      "main": [
        [
          {
            "node": "FE - History Merge",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "FE - History Merge": {
      "main": [
        [
          {
            "node": "FE - Store Iterations",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FE - Store Iterations": {
      "main": [
        [
          {
            "node": "FE - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "FE - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FE - Review": {
      "main": [
        [
          {
            "node": "FE - Review Context",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "FE - Review Context": {
      "main": [
        [
          {
            "node": "FE - Restore Older Iteration?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FE - Restore Older Iteration?": {
      "main": [
        [
          {
            "node": "FE - Restore Selected Iteration",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "FE - Approved?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FE - Restore Selected Iteration": {
      "main": [
        [
          {
            "node": "FE - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "FE - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FE - Approved?": {
      "main": [
        [
          {
            "node": "FE - End",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "FE - Prepare Refine Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FE - Prepare Refine Input": {
      "main": [
        [
          {
            "node": "FE - History Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "FE - Create Frontend",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "INF - Start": {
      "main": [
        [
          {
            "node": "INF - Create Infrastructure",
            "type": "main",
            "index": 0
          },
          {
            "node": "INF - History Context",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "INF - Create Infrastructure": {
      "main": [
        [
          {
            "node": "INF - History Merge",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "INF - History Context": {
      "main": [
        [
          {
            "node": "INF - History Merge",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "INF - History Merge": {
      "main": [
        [
          {
            "node": "INF - Store Iterations",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "INF - Store Iterations": {
      "main": [
        [
          {
            "node": "INF - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "INF - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "INF - Review": {
      "main": [
        [
          {
            "node": "INF - Review Context",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "INF - Review Context": {
      "main": [
        [
          {
            "node": "INF - Restore Older Iteration?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "INF - Restore Older Iteration?": {
      "main": [
        [
          {
            "node": "INF - Restore Selected Iteration",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "INF - Approved?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "INF - Restore Selected Iteration": {
      "main": [
        [
          {
            "node": "INF - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "INF - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "INF - Approved?": {
      "main": [
        [
          {
            "node": "INF - End",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "INF - Prepare Refine Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "INF - Prepare Refine Input": {
      "main": [
        [
          {
            "node": "INF - History Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "INF - Create Infrastructure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "TEST - Start": {
      "main": [
        [
          {
            "node": "TEST - Create Tests",
            "type": "main",
            "index": 0
          },
          {
            "node": "TEST - History Context",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "TEST - Create Tests": {
      "main": [
        [
          {
            "node": "TEST - History Merge",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "TEST - History Context": {
      "main": [
        [
          {
            "node": "TEST - History Merge",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "TEST - History Merge": {
      "main": [
        [
          {
            "node": "TEST - Store Iterations",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "TEST - Store Iterations": {
      "main": [
        [
          {
            "node": "TEST - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "TEST - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "TEST - Review": {
      "main": [
        [
          {
            "node": "TEST - Review Context",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "TEST - Review Context": {
      "main": [
        [
          {
            "node": "TEST - Restore Older Iteration?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "TEST - Restore Older Iteration?": {
      "main": [
        [
          {
            "node": "TEST - Restore Selected Iteration",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "TEST - Approved?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "TEST - Restore Selected Iteration": {
      "main": [
        [
          {
            "node": "TEST - Review Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "TEST - Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "TEST - Approved?": {
      "main": [
        [
          {
            "node": "TEST - End",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "TEST - Prepare Refine Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "TEST - Prepare Refine Input": {
      "main": [
        [
          {
            "node": "TEST - History Context",
            "type": "main",
            "index": 0
          },
          {
            "node": "TEST - Create Tests",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FE - End": {
      "main": [
        [
          {
            "node": "Flow Node5",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "INF - End": {
      "main": [
        [
          {
            "node": "Flow Node6",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Flow Node3": {
      "main": [
        [
          {
            "node": "BE - Start",
            "type": "main",
            "index": 0
          },
          {
            "node": "FE - Input Merge",
            "type": "main",
            "index": 1
          },
          {
            "node": "Flow Node7",
            "type": "main",
            "index": 0
          },
          {
            "node": "FINAL - Merge Artifacts",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "Flow Node5": {
      "main": [
        [
          {
            "node": "INF - Input Merge",
            "type": "main",
            "index": 0
          },
          {
            "node": "Flow Node10",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Flow Node6": {
      "main": [
        [
          {
            "node": "TEST - Input Merge",
            "type": "main",
            "index": 0
          },
          {
            "node": "Flow Node13",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "INF - Input Merge": {
      "main": [
        [
          {
            "node": "INF - Start",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "TEST - Input Merge": {
      "main": [
        [
          {
            "node": "TEST - Start",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Flow Node7": {
      "main": [
        [
          {
            "node": "INF - Input Merge",
            "type": "main",
            "index": 1
          },
          {
            "node": "Flow Node8",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Flow Node8": {
      "main": [
        [
          {
            "node": "TEST - Input Merge",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "TEST - End": {
      "main": [
        [
          {
            "node": "Flow Node11",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Flow Node9": {
      "main": [
        [
          {
            "node": "FINAL - Merge Artifacts",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Flow Node10": {
      "main": [
        [
          {
            "node": "FINAL - Merge Artifacts",
            "type": "main",
            "index": 2
          }
        ]
      ]
    },
    "Flow Node11": {
      "main": [
        [
          {
            "node": "Flow Node12",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Flow Node12": {
      "main": [
        [
          {
            "node": "FINAL - Merge Artifacts",
            "type": "main",
            "index": 4
          }
        ]
      ]
    },
    "Flow Node13": {
      "main": [
        [
          {
            "node": "FINAL - Merge Artifacts",
            "type": "main",
            "index": 3
          }
        ]
      ]
    },
    "FINAL - Merge Artifacts": {
      "main": [
        [
          {
            "node": "FINAL - Prepare Static Artifacts",
            "type": "main",
            "index": 0
          },
          {
            "node": "FINAL - Prepare Module ZIP Metadata",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Prepare Module ZIP Metadata": {
      "main": [
        [
          {
            "node": "FINAL - Unpack Backend ZIP",
            "type": "main",
            "index": 0
          },
          {
            "node": "FINAL - Unpack Frontend ZIP",
            "type": "main",
            "index": 0
          },
          {
            "node": "FINAL - Unpack Infrastructure ZIP",
            "type": "main",
            "index": 0
          },
          {
            "node": "FINAL - Unpack Tests ZIP",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Unpack Backend ZIP": {
      "main": [
        [
          {
            "node": "FINAL - Normalize Backend Files",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Unpack Frontend ZIP": {
      "main": [
        [
          {
            "node": "FINAL - Normalize Frontend Files",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Unpack Infrastructure ZIP": {
      "main": [
        [
          {
            "node": "FINAL - Normalize Infrastructure Files",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Unpack Tests ZIP": {
      "main": [
        [
          {
            "node": "FINAL - Normalize Tests Files",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Prepare Static Artifacts": {
      "main": [
        [
          {
            "node": "FINAL - Merge Bundle Parts",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Normalize Backend Files": {
      "main": [
        [
          {
            "node": "FINAL - Merge Bundle Parts",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "FINAL - Normalize Frontend Files": {
      "main": [
        [
          {
            "node": "FINAL - Merge Bundle Parts",
            "type": "main",
            "index": 2
          }
        ]
      ]
    },
    "FINAL - Normalize Infrastructure Files": {
      "main": [
        [
          {
            "node": "FINAL - Merge Bundle Parts",
            "type": "main",
            "index": 3
          }
        ]
      ]
    },
    "FINAL - Normalize Tests Files": {
      "main": [
        [
          {
            "node": "FINAL - Merge Bundle Parts",
            "type": "main",
            "index": 4
          }
        ]
      ]
    },
    "FINAL - Merge Bundle Parts": {
      "main": [
        [
          {
            "node": "FINAL - Build Bundle Files",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Build Bundle Files": {
      "main": [
        [
          {
            "node": "FINAL - Create Master ZIP",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Create Master ZIP": {
      "main": [
        [
          {
            "node": "FINAL - Prepare Zip Link Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Prepare Zip Link Input": {
      "main": [
        [
          {
            "node": "FINAL - Create ZIP Link",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Prepare Completion Page": {
      "main": [
        [
          {
            "node": "FINAL - Done",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "FINAL - Create ZIP Link": {
      "main": [
        [
          {
            "node": "FINAL - Prepare Completion Page",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate",
    "availableInMCP": false
  },
  "versionId": "72f32c0d-7f98-4509-82f7-c0f6db4deff5",
  "meta": {
    "instanceId": "af49a67438f7e7c513a029a95d0dadb7f585243236e86464fcac385b6f1438c3"
  },
  "id": "YiifzvALf6abhZnB",
  "tags": []
}